# =====================================================
# ReviewGuide Frontend - ECS Environment Variables
# =====================================================
# Copy this file and update values for your ECS Task Definition
#
# NOTE: NEXT_PUBLIC_* variables are embedded at BUILD TIME
# You must rebuild the Docker image when changing these values
# =====================================================

# ===================
# API Configuration
# ===================
# Backend API URL - Update with your actual domain
# This is baked into the build, so set correctly before building
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# ===================
# Runtime Settings
# ===================
# These can be set at runtime in ECS task definition
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1