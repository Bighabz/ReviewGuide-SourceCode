---
phase: 14-chat-screen
plan: 04
type: execute
wave: 4
depends_on: [14-03]
files_modified: []
autonomous: false
requirements: [CHAT-01, CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-06]

must_haves:
  truths:
    - "Human has visually verified the chat screen on mobile and desktop"
    - "All 6 CHAT requirements confirmed working by manual testing"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 14 chat screen. Confirm all visual and interactive behaviors match the spec before marking the phase complete.

Purpose: Automated tests verify structure but cannot assess visual quality, real streaming behavior, or cross-device rendering. This checkpoint catches visual regressions.
Output: Human approval or issue list for gap closure.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-chat-screen/14-CONTEXT.md
@.planning/phases/14-chat-screen/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build check</name>
  <files></files>
  <action>
Run the complete frontend test suite and a production build to confirm no regressions.

1. `cd frontend && npx vitest run` — all tests must pass (172+ existing + new Phase 14 tests)
2. `cd frontend && npm run build` — must complete without errors

If any tests fail or build breaks, fix the issues before proceeding to human verification.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite GREEN. Production build passes cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete chat screen</name>
  <files></files>
  <action>
Complete Phase 14 chat screen redesign built: message bubbles with iMessage-style corners, ReviewGuide byline on AI messages, compact inline product cards, source citations with colored dots, horizontal pill-style suggestion chips, and MobileHeader with dynamic title and streaming status.

**Prerequisites:** Start backend: `docker-compose up -d`. Start frontend: `cd frontend && npm run dev`.

**CHAT-05 — Bubble styling (mobile + desktop):**
1. Open http://localhost:3000/chat on mobile viewport (Chrome DevTools, 390px width)
2. Type a message and send it
3. Verify: your message appears RIGHT-aligned in a BLUE bubble with rounded corners and a small tail on the bottom-right
4. Verify: AI response appears LEFT-aligned in a WHITE bubble with a thin border, "ReviewGuide" label at the top
5. Verify the asymmetric corners are visible (iMessage-style)

**CHAT-01 — Response structure:**
6. Send a product query like "best noise cancelling headphones under $300"
7. Verify the AI response renders in this order: (1) editorial summary text, (2) compact product cards, (3) source citations, (4) suggestion chips
8. Verify all blocks are INSIDE the white AI bubble except chips (chips are outside)

**CHAT-02 — Inline product cards:**
9. Verify product cards are compact horizontal rows (~64px tall)
10. Verify each card shows: rank badge (Top Pick / Best Value / Premium), product image, name, price, "Buy on Amazon" link
11. Click a "Buy on Amazon" link — verify it opens a working URL

**CHAT-04 — Source citations:**
12. Verify a "Sources" section appears with colored dots (red, blue, green, orange)
13. Verify each source name is clickable and opens the original review article in a new tab
14. If more than 4 sources, verify "+X more" expander works

**CHAT-03 — Header status:**
15. Send another query and watch the MobileHeader
16. Verify: header title shows the first user message (truncated)
17. Verify: during streaming, a status line appears below the title (e.g., "Researching..." or tool status text)
18. Verify: status line disappears when streaming completes

**CHAT-06 — Suggestion chips:**
19. Verify suggestion chips appear as horizontal pills with blue border and blue text
20. Tap a chip — verify it sends the text as the next user message without typing
21. Verify chips are outside the AI bubble (visually separated)

**Desktop check:**
22. Widen viewport to desktop (1200px+)
23. Verify all the above still works — bubbles, cards, citations, chips
24. Verify MobileHeader is NOT visible on desktop (UnifiedTopbar shows instead)
  </action>
  <verify>Type "approved" to complete Phase 14, or describe issues for gap closure</verify>
  <done>Human has visually confirmed all 6 CHAT requirements on mobile and desktop viewports.</done>
</task>

</tasks>

<verification>
Human confirmation of all 6 CHAT requirements across mobile and desktop viewports.
</verification>

<success_criteria>
- Human types "approved" after verifying all behaviors
- OR human provides specific issue descriptions for gap closure plans
</success_criteria>

<output>
After completion, create `.planning/phases/14-chat-screen/14-04-SUMMARY.md`
</output>
