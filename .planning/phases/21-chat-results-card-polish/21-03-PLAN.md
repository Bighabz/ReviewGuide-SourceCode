---
phase: 21-chat-results-card-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/ResultsProductCard.tsx
  - frontend/components/ResultsMainPanel.tsx
  - frontend/tests/resultsScreen.test.tsx
autonomous: true
requirements: [RES-07, RES-08, CARD-04]
must_haves:
  truths:
    - "Results page product cards have spring hover lift and bolder typography"
    - "Results page source dots are larger (w-3 h-3) with ring emphasis"
    - "ResultsProductCard uses Framer Motion spring hover, not CSS product-card-hover"
  artifacts:
    - path: "frontend/components/ResultsProductCard.tsx"
      provides: "Bold v3.0 ResultsProductCard with motion.div spring hover, bolder badge/type"
      contains: "motion.div"
    - path: "frontend/components/ResultsMainPanel.tsx"
      provides: "Upgraded source dots with larger size and ring treatment"
      contains: "w-3 h-3"
    - path: "frontend/tests/resultsScreen.test.tsx"
      provides: "Extended test assertions for dot size and badge styling"
  key_links:
    - from: "frontend/components/ResultsProductCard.tsx"
      to: "framer-motion"
      via: "motion.div import and whileHover prop"
      pattern: "whileHover"
    - from: "frontend/components/ResultsMainPanel.tsx"
      to: "frontend/app/globals.css"
      via: "SOURCE_COLORS array maps to bold visual presence"
      pattern: "SOURCE_COLORS"
---

<objective>
Upgrade ResultsProductCard with bold v3.0 visual language and spring hover animations. Upgrade ResultsMainPanel source dots with larger size and ring emphasis. Extend existing resultsScreen tests.

Purpose: Complete the visual polish for the results page, ensuring product cards and source citations match the v3.0 bold editorial language.
Output: Two upgraded results components with spring physics and bolder visual treatments, plus extended test coverage.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-chat-results-card-polish/21-RESEARCH.md

<interfaces>
<!-- Current component state the executor needs. -->

From frontend/components/ResultsProductCard.tsx — current outer div (lines 40-43):
```typescript
<div
  className="rounded-2xl border overflow-hidden product-card-hover transition-all duration-200 hover:shadow-card-hover hover:-translate-y-0.5"
  style={{ background: 'var(--surface)', borderColor: 'var(--border)' }}
>
```

From frontend/components/ResultsProductCard.tsx — getBadge function (lines 23-28):
```typescript
function getBadge(index: number): { label: string; bg: string; color: string } {
  if (index === 0) return { label: 'TOP PICK', bg: '#FEF3C7', color: '#92400E' }
  if (index === 1) return { label: 'BEST VALUE', bg: '#DBEAFE', color: '#1E40AF' }
  if (index === 2) return { label: 'PREMIUM', bg: '#F3E8FF', color: '#6B21A8' }
  return { label: `#${index + 1}`, bg: 'var(--surface-elevated)', color: 'var(--text-secondary)' }
}
```

From frontend/components/ResultsProductCard.tsx — product name heading (line 79):
```typescript
<h3 className="text-[15px] font-semibold mb-1" style={{ color: 'var(--text)' }}>
```

From frontend/components/ResultsMainPanel.tsx — source dots (line 142):
```typescript
<span className="w-2 h-2 rounded-full flex-shrink-0" style={{ backgroundColor: SOURCE_COLORS[idx % SOURCE_COLORS.length] }} />
```

From frontend/components/ResultsMainPanel.tsx — SOURCE_COLORS (line 60):
```typescript
const SOURCE_COLORS = ['#EF4444', '#3B82F6', '#10B981', '#F59E0B', '#8B5CF6', '#EC4899']
```

From frontend/lib/extractResultsData.ts (type used by ResultsProductCard):
```typescript
export interface ExtractedProduct {
  name: string
  price?: number
  url?: string
  image_url?: string
  merchant?: string
  description?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ResultsProductCard with bold v3.0 visual language and spring hover (RES-07, CARD-04)</name>
  <files>frontend/components/ResultsProductCard.tsx</files>
  <action>
**1. Add framer-motion import:**
```typescript
import { motion } from 'framer-motion'
```

**2. Convert outer `<div>` to `<motion.div>`** with spring hover. Remove `product-card-hover`, `transition-all`, `duration-200`, `hover:shadow-card-hover`, and `hover:-translate-y-0.5` from className — Framer Motion now owns the hover transform.

```typescript
<motion.div
  whileHover={{
    y: -4,
    boxShadow: '0 12px 32px rgba(28,25,23,0.10)',
  }}
  transition={{ type: 'spring', stiffness: 400, damping: 28 }}
  className="rounded-2xl border overflow-hidden"
  style={{ background: 'var(--surface)', borderColor: 'var(--border)' }}
>
```

**3. Upgrade product name typography** (currently line 79):
```typescript
// Before:
<h3 className="text-[15px] font-semibold mb-1" style={{ color: 'var(--text)' }}>

// After:
<h3 className="text-base font-bold mb-1 font-serif tracking-tight" style={{ color: 'var(--text)' }}>
```

Changes: `text-[15px]` to `text-base` (16px), `font-semibold` to `font-bold`, added `font-serif tracking-tight` for editorial heading feel.

**4. Upgrade badge colors to use V3 bold tokens** — update getBadge function:
```typescript
function getBadge(index: number): { label: string; bg: string; color: string } {
  if (index === 0) return { label: 'TOP PICK', bg: 'var(--bold-amber)', color: '#FFFFFF' }
  if (index === 1) return { label: 'BEST VALUE', bg: 'var(--bold-blue)', color: '#FFFFFF' }
  if (index === 2) return { label: 'PREMIUM', bg: 'var(--bold-green)', color: '#FFFFFF' }
  return { label: `#${index + 1}`, bg: 'var(--surface-elevated)', color: 'var(--text-secondary)' }
}
```

This uses the V3 `--bold-*` tokens declared in globals.css with white text for strong contrast.

**5. Upgrade badge element styling** — make it bolder:
```typescript
// Before:
<span className="inline-block px-2 py-0.5 rounded text-[10px] font-semibold uppercase tracking-wide mb-2"

// After:
<span className="inline-block px-2.5 py-1 rounded-md text-[10px] font-bold uppercase tracking-wider mb-2"
```

Changes: `px-2` to `px-2.5`, `py-0.5` to `py-1`, `rounded` to `rounded-md`, `font-semibold` to `font-bold`, `tracking-wide` to `tracking-wider` for stronger badge presence.

**6. Do NOT add `layout` prop** — ResultsProductCard is not inside a streaming container, but consistency with other cards.
  </action>
  <verify>
    <automated>cd /c/Users/habib/downloads/reviewguide-sourcecode/frontend && npm run test:run -- tests/resultsScreen.test.tsx tests/cardAnimations.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>ResultsProductCard uses motion.div with spring hover (stiffness: 400, damping: 28). No product-card-hover or CSS hover translate classes. Product name is font-bold font-serif. Badge colors use V3 --bold-* tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade ResultsMainPanel source dots and extend resultsScreen tests (RES-08)</name>
  <files>frontend/components/ResultsMainPanel.tsx, frontend/tests/resultsScreen.test.tsx</files>
  <action>
**ResultsMainPanel.tsx changes:**

**1. Upgrade source dots** from `w-2 h-2` to `w-3 h-3` with a ring treatment for stronger visual presence. Find the source dot span (around line 142):

```typescript
// Before:
<span className="w-2 h-2 rounded-full flex-shrink-0" style={{ backgroundColor: SOURCE_COLORS[idx % SOURCE_COLORS.length] }} />

// After:
<span
  className="w-3 h-3 rounded-full flex-shrink-0 ring-2 ring-offset-1"
  style={{
    backgroundColor: SOURCE_COLORS[idx % SOURCE_COLORS.length],
    ['--tw-ring-color' as string]: SOURCE_COLORS[idx % SOURCE_COLORS.length] + '40',
    ['--tw-ring-offset-color' as string]: 'var(--surface)',
  }}
/>
```

The ring uses the same color at 25% opacity (`40` hex suffix) for a subtle glow effect. The `ring-offset-color` uses `var(--surface)` so the offset matches the background in both light and dark mode.

Note: If the TypeScript style typing is problematic, an alternative approach is to add a utility class in the className and skip the ring-color inline style. In that case, just use:
```typescript
<span
  className="w-3 h-3 rounded-full flex-shrink-0"
  style={{ backgroundColor: SOURCE_COLORS[idx % SOURCE_COLORS.length] }}
/>
```
This is the minimum viable approach — larger dots alone provide stronger visual presence. Add the ring treatment only if TypeScript allows the inline style approach cleanly.

**2. Upgrade source site name typography** — make the site name bolder:
```typescript
// Before:
<span className="font-medium" style={{ color: 'var(--text)' }}>{source.site_name}</span>

// After:
<span className="font-semibold" style={{ color: 'var(--text)' }}>{source.site_name}</span>
```

**resultsScreen.test.tsx changes:**

Add new assertions to existing tests (or add a new describe block):
- Test "source dots are w-3 h-3" — find source dot elements by their rounded-full class, verify they have `w-3` and `h-3` classes (not `w-2` and `h-2`)
- Test "source site names use font-semibold" — find site name elements, verify they have `font-semibold` class
- Test "product cards do not have product-card-hover class" — find ResultsProductCard elements, verify none have `product-card-hover` in their className

Use existing test patterns from the file — the resultsScreen.test.tsx file already renders ResultsMainPanel with mock messages.
  </action>
  <verify>
    <automated>cd /c/Users/habib/downloads/reviewguide-sourcecode/frontend && npm run test:run -- tests/resultsScreen.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <done>Source dots are w-3 h-3 (50% larger). Source site names use font-semibold. Tests verify dot size and typography upgrades. No regressions in existing results screen tests.</done>
</task>

</tasks>

<verification>
Run the full frontend test suite to confirm no regressions:
```bash
cd frontend && npm run test:run
```
Verify build still passes:
```bash
cd frontend && npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- ResultsProductCard uses motion.div with spring hover, no CSS hover classes
- ResultsProductCard product name is font-bold font-serif tracking-tight
- ResultsProductCard badges use V3 --bold-* token colors with white text
- Source dots are w-3 h-3 (not w-2 h-2)
- Source site names are font-semibold
- Tests verify the visual upgrades
- All existing frontend tests still pass
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/21-chat-results-card-polish/21-03-SUMMARY.md`
</output>
