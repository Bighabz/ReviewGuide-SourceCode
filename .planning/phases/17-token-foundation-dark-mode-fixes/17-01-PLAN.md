---
phase: 17-token-foundation-dark-mode-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/globals.css
autonomous: true
requirements:
  - TOK-01
  - TOK-02
  - TOK-03

must_haves:
  truths:
    - "New bold accent color tokens (--bold-blue, --bold-green, --bold-red, --bold-amber) are visible on :root in browser DevTools"
    - "Typography scale tokens (--heading-hero, --heading-xl, --heading-lg, --heading-md, --heading-sm) are defined as CSS custom properties"
    - "Every new token added to :root has a matching value in [data-theme='dark']"
    - "Pre-existing missing dark mode tokens (--error, --warning, --info, --shadow-xl) now have [data-theme='dark'] counterparts"
    - "All 13 existing designTokens.test.ts assertions still pass"
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "Bold accent palette, typography scale tokens, dark mode gap fixes"
      contains: "--bold-green"
  key_links:
    - from: "frontend/app/globals.css :root"
      to: "frontend/app/globals.css [data-theme='dark']"
      via: "Every new --var-name in :root has a counterpart in dark block"
      pattern: "--bold-(blue|green|red|amber)"
---

<objective>
Add bold editorial accent color tokens, typography scale tokens, and missing dark mode counterparts to the CSS custom property system in globals.css.

Purpose: Establish the token foundation that all subsequent v3.0 visual phases build on. Without these tokens, component conversions in Plan 02 and styling upgrades in Phases 20-21 have nothing to reference.
Output: Updated `frontend/app/globals.css` with new token blocks in both `:root` and `[data-theme="dark"]`.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-token-foundation-dark-mode-fixes/17-RESEARCH.md

<interfaces>
<!-- Key structure the executor needs from globals.css -->
<!-- The file has two main token blocks: :root (lines 5-102) and [data-theme="dark"] (lines 104-156) -->
<!-- New tokens MUST be added to BOTH blocks in the same edit -->

Current :root token structure (lines 5-102):
- Primary colors: --primary, --primary-hover, --primary-light
- Accent colors: --accent, --accent-hover, --accent-light, --success, --success-light
- Semantic: --error (#D93025), --warning (#E5A100), --info (#1A73E8)
- Backgrounds: --background, --surface, --surface-hover, --surface-elevated, --surface-float
- Text: --text, --text-secondary, --text-muted
- Borders: --border, --border-strong
- Shadows: --shadow-sm, --shadow-md, --shadow-lg, --shadow-xl, --shadow-float
- Pricing: --price-deal, --rating-star
- Card accents: --card-accent-1 through --card-accent-4
- Legacy mappings: --gpt-* variables
- Streaming tokens: --stream-status-*, --stream-content-*, --citation-*

Current [data-theme="dark"] block (lines 104-156):
- Has overrides for: --primary, --accent, --success, --background, --surface, --text, --border, --shadow-sm/md/lg/float, --price-deal, --rating-star, --card-accent-*
- MISSING dark overrides for: --error, --warning, --info, --shadow-xl

Current heading sizes in global rules (lines 249-251):
  h1 { font-size: 2.5rem; }
  h2 { font-size: 1.875rem; }
  h3 { font-size: 1.375rem; }
DO NOT modify these global h1/h2/h3 rules. Typography tokens are named utilities applied explicitly by downstream components.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bold accent palette and fix missing dark mode gaps in globals.css</name>
  <files>frontend/app/globals.css</files>
  <action>
Edit `frontend/app/globals.css` to add new token blocks. All additions go INSIDE the existing `:root` and `[data-theme="dark"]` blocks — do NOT create new blocks.

**In `:root` block (after the `--card-accent-4` line, before Legacy Mappings):**

Add a new comment section and these tokens:

```css
/* ═══════════════════════════════════════════
   V3 BOLD EDITORIAL ACCENTS
   Vibrant, energetic — for product card labels, badges, CTAs
   ═══════════════════════════════════════════ */
--bold-blue: #1B4DFF;
--bold-green: #16A34A;
--bold-red: #DC2626;
--bold-amber: #D97706;
```

**In `[data-theme="dark"]` block (after `--rating-star` line, before closing brace):**

Add the missing dark mode overrides for EXISTING tokens AND the new bold accent tokens:

```css
/* Missing dark overrides (pre-existing gaps) */
--error: #F87171;
--warning: #FCD34D;
--info: #60A5FA;
--shadow-xl: 0 20px 48px rgba(0, 0, 0, 0.7), 0 8px 16px rgba(0, 0, 0, 0.4);

/* V3 Bold accents — dark mode */
--bold-blue: #60A5FA;
--bold-green: #4ADE80;
--bold-red: #F87171;
--bold-amber: #FCD34D;
```

CRITICAL RULES:
- Do NOT rename any existing `--gpt-*` or `--stream-*` variables
- Do NOT modify values of existing tokens
- Do NOT touch anything outside the `:root` and `[data-theme="dark"]` blocks
- Every new variable in `:root` MUST have a counterpart in `[data-theme="dark"]`
  </action>
  <verify>
    <automated>cd frontend && npm run test:run -- --reporter=verbose tests/designTokens.test.ts</automated>
  </verify>
  <done>All 13 existing design token tests pass. New bold accent tokens (--bold-blue, --bold-green, --bold-red, --bold-amber) are present in both :root and [data-theme="dark"]. Pre-existing dark mode gaps (--error, --warning, --info, --shadow-xl) are closed.</done>
</task>

<task type="auto">
  <name>Task 2: Add typography scale tokens to globals.css</name>
  <files>frontend/app/globals.css</files>
  <action>
Edit `frontend/app/globals.css` to add typography scale tokens. These go in BOTH the `:root` and `[data-theme="dark"]` blocks.

**In `:root` block (after the bold accent tokens just added in Task 1):**

```css
/* ═══════════════════════════════════════════
   V3 TYPOGRAPHY SCALE
   Named heading tokens — applied explicitly by components, not global overrides
   ═══════════════════════════════════════════ */
--heading-hero: clamp(2.5rem, 5vw, 4.5rem);
--heading-xl: clamp(1.875rem, 3vw, 2.5rem);
--heading-lg: 1.875rem;
--heading-md: 1.375rem;
--heading-sm: 1.125rem;
--heading-weight: 700;
--heading-line-height: 1.15;
```

**In `[data-theme="dark"]` block (after the bold accent dark tokens):**

Typography tokens are theme-neutral (no luminance dependence), but they MUST still be declared in the dark block so downstream code can safely reference them without conditional logic:

```css
/* V3 Typography — same values, declared for completeness */
--heading-hero: clamp(2.5rem, 5vw, 4.5rem);
--heading-xl: clamp(1.875rem, 3vw, 2.5rem);
--heading-lg: 1.875rem;
--heading-md: 1.375rem;
--heading-sm: 1.125rem;
--heading-weight: 700;
--heading-line-height: 1.15;
```

CRITICAL: Do NOT modify the existing global `h1 { font-size: 2.5rem; }`, `h2`, `h3` rules (lines 249-251). The typography tokens are named utilities that Phase 20 components will apply explicitly via `text-[var(--heading-hero)]`. Changing the global h1 rule would affect chat headings.
  </action>
  <verify>
    <automated>cd frontend && npm run test:run -- --reporter=verbose tests/designTokens.test.ts</automated>
  </verify>
  <done>All 13 existing design token tests pass. Typography tokens (--heading-hero through --heading-sm, --heading-weight, --heading-line-height) are present in both :root and [data-theme="dark"]. The global h1/h2/h3 rules remain unchanged.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd frontend && npm run test:run -- tests/designTokens.test.ts` — 13/13 pass (no regressions)
2. `cd frontend && npm run test:run` — full suite does not introduce any NEW failures beyond the 21 pre-existing ones
3. `grep -c "bold-blue\|bold-green\|bold-red\|bold-amber" frontend/app/globals.css` returns at least 8 (4 in :root + 4 in dark)
4. `grep -c "heading-hero\|heading-xl\|heading-lg\|heading-md\|heading-sm" frontend/app/globals.css` returns at least 10 (5 in :root + 5 in dark)
</verification>

<success_criteria>
- All 13 existing designTokens.test.ts assertions pass
- Bold accent tokens (--bold-blue, --bold-green, --bold-red, --bold-amber) exist in :root and [data-theme="dark"]
- Typography scale tokens (--heading-hero, --heading-xl, --heading-lg, --heading-md, --heading-sm, --heading-weight, --heading-line-height) exist in :root and [data-theme="dark"]
- Missing dark mode gaps (--error, --warning, --info, --shadow-xl) are closed
- No existing token values were modified
- No --gpt-* or --stream-* tokens were renamed
- Global h1/h2/h3 rules unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/17-token-foundation-dark-mode-fixes/17-01-SUMMARY.md`
</output>
