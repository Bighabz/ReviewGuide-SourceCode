---
phase: 17-token-foundation-dark-mode-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - frontend/components/ProductReview.tsx
  - frontend/components/TopPickBlock.tsx
  - frontend/components/ProductCards.tsx
autonomous: true
requirements:
  - TOK-03

must_haves:
  truths:
    - "Toggling dark mode does not produce any white/light color flashes on ProductReview pros/cons labels"
    - "Toggling dark mode does not produce any white/light color flashes on TopPickBlock best-for/pros/cons labels"
    - "Toggling dark mode does not produce any white/light color flashes on ProductCards pros/cons labels"
    - "All 9 hardcoded Tailwind color classes (text-green-700, text-green-600, text-red-700, text-red-600, text-emerald-600, text-red-500) are replaced with var() references"
    - "No dark: Tailwind utility prefixes remain in the three target components"
  artifacts:
    - path: "frontend/components/ProductReview.tsx"
      provides: "Pros/cons labels using semantic tokens"
      contains: "var(--success)"
    - path: "frontend/components/TopPickBlock.tsx"
      provides: "Best-for/pros/cons labels using semantic tokens"
      contains: "var(--success)"
    - path: "frontend/components/ProductCards.tsx"
      provides: "What-we-like/watch-out labels using semantic tokens"
      contains: "var(--success)"
  key_links:
    - from: "frontend/components/ProductReview.tsx"
      to: "frontend/app/globals.css"
      via: "text-[var(--success)] and text-[var(--error)] class references"
      pattern: "text-\\[var\\(--success\\)\\]|text-\\[var\\(--error\\)\\]"
    - from: "frontend/components/TopPickBlock.tsx"
      to: "frontend/app/globals.css"
      via: "text-[var(--success)] and text-[var(--error)] class references"
      pattern: "text-\\[var\\(--success\\)\\]|text-\\[var\\(--error\\)\\]"
    - from: "frontend/components/ProductCards.tsx"
      to: "frontend/app/globals.css"
      via: "text-[var(--success)] and text-[var(--error)] class references"
      pattern: "text-\\[var\\(--success\\)\\]|text-\\[var\\(--error\\)\\]"
---

<objective>
Convert all 9 hardcoded Tailwind color classes in ProductReview.tsx, TopPickBlock.tsx, and ProductCards.tsx to semantic CSS variable references that respect the [data-theme="dark"] dark mode strategy.

Purpose: Eliminate dark mode regressions where hardcoded green/red Tailwind classes render on dark backgrounds. After this plan, all pros/cons/sentiment labels in product card leaf components use the token system established in Plan 01.
Output: Three updated component files with zero hardcoded color palette classes for sentiment labels.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-token-foundation-dark-mode-fixes/17-RESEARCH.md
@.planning/phases/17-token-foundation-dark-mode-fixes/17-01-SUMMARY.md

<interfaces>
<!-- Tokens available from Plan 01 (in globals.css) that this plan references -->

:root values:
  --success: #1A8754   (dark forest green — for pros labels)
  --error: #D93025     (strong red — for cons labels)

[data-theme="dark"] values:
  --success: #22C55E   (bright green — readable on dark background)
  --error: #F87171     (bright red — readable on dark background)

Established codebase pattern for token references in className:
  className="text-[var(--success)]"
  className="text-[var(--error)]"
  className="text-[var(--text)]"
  className="text-[var(--primary)]"

This project uses data-theme="dark" on html element, NOT Tailwind dark: class.
Therefore dark: utilities (e.g. dark:text-emerald-400) are SILENTLY INERT and must be removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert ProductReview.tsx hardcoded colors to token references</name>
  <files>frontend/components/ProductReview.tsx</files>
  <action>
Replace 4 hardcoded Tailwind color classes in `frontend/components/ProductReview.tsx`:

**Line 108** — Pros heading:
```
BEFORE: text-green-700
AFTER:  text-[var(--success)]
```

**Line 115** — Pros checkmark icon:
```
BEFORE: text-green-600
AFTER:  text-[var(--success)]
```

**Line 126** — Cons heading:
```
BEFORE: text-red-700
AFTER:  text-[var(--error)]
```

**Line 133** — Cons X icon:
```
BEFORE: text-red-600
AFTER:  text-[var(--error)]
```

Do NOT modify any other part of the component. Do NOT touch the affiliate links section, the rating stars, or the image rendering. Only the 4 sentiment color classes change.

After editing, verify with grep that no `text-green-` or `text-red-` (Tailwind palette) classes remain in this file. The `text-amber-500` on the rating star is intentional and should NOT be converted (it is a fixed brand color for stars).
  </action>
  <verify>
    <automated>cd frontend && grep -n "text-green-\|text-red-" components/ProductReview.tsx; echo "EXIT:$?"</automated>
  </verify>
  <done>grep returns zero matches (exit code 1 = no match). All 4 hardcoded green/red classes in ProductReview.tsx are replaced with text-[var(--success)] or text-[var(--error)].</done>
</task>

<task type="auto">
  <name>Task 2: Convert TopPickBlock.tsx hardcoded colors and remove inert dark: utility</name>
  <files>frontend/components/TopPickBlock.tsx</files>
  <action>
Replace 3 hardcoded Tailwind color classes and remove 1 inert `dark:` utility in `frontend/components/TopPickBlock.tsx`:

**Line 135** — Best-for label:
```
BEFORE: text-emerald-600 dark:text-emerald-400
AFTER:  text-[var(--success)]
```
Note: Remove BOTH `text-emerald-600` AND `dark:text-emerald-400`. The `dark:` prefix is silently inert with data-theme strategy and must be deleted entirely.

**Line 160** — Pros label:
```
BEFORE: text-green-600
AFTER:  text-[var(--success)]
```

**Line 166** — Cons label:
```
BEFORE: text-red-500
AFTER:  text-[var(--error)]
```

Do NOT modify any other part of the component. Do NOT touch the CTA button gradient, the rating stars, the chevron buttons, or the "Look elsewhere if" label (which correctly uses `text-[var(--accent)]` already).

After editing, verify with grep that no `text-green-`, `text-red-`, `text-emerald-`, or `dark:` classes remain in this file. The `text-amber-500` on rating stars is intentional and should NOT be converted.
  </action>
  <verify>
    <automated>cd frontend && grep -n "text-green-\|text-red-\|text-emerald-\|dark:" components/TopPickBlock.tsx; echo "EXIT:$?"</automated>
  </verify>
  <done>grep returns zero matches (exit code 1 = no match). All 3 hardcoded classes plus the inert dark:text-emerald-400 are replaced with text-[var(--success)] or text-[var(--error)]. No dark: utilities remain.</done>
</task>

<task type="auto">
  <name>Task 3: Convert ProductCards.tsx hardcoded colors to token references</name>
  <files>frontend/components/ProductCards.tsx</files>
  <action>
Replace 2 hardcoded Tailwind color classes in `frontend/components/ProductCards.tsx`:

**Line 125** — "What we like" label:
```
BEFORE: text-green-600
AFTER:  text-[var(--success)]
```

**Line 131** — "Watch out for" label:
```
BEFORE: text-red-500
AFTER:  text-[var(--error)]
```

Do NOT modify any other part of the component. Do NOT touch the CTA button gradient, the product image, the price display, or the badges.

After editing, verify with grep that no `text-green-` or `text-red-` (Tailwind palette) classes remain in this file.
  </action>
  <verify>
    <automated>cd frontend && grep -n "text-green-\|text-red-" components/ProductCards.tsx; echo "EXIT:$?"</automated>
  </verify>
  <done>grep returns zero matches (exit code 1 = no match). Both hardcoded classes in ProductCards.tsx are replaced with text-[var(--success)] or text-[var(--error)].</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `cd frontend && grep -rn "text-green-\|text-red-\|text-emerald-" components/ProductReview.tsx components/TopPickBlock.tsx components/ProductCards.tsx` — returns zero matches
2. `cd frontend && grep -rn "dark:" components/ProductReview.tsx components/TopPickBlock.tsx components/ProductCards.tsx` — returns zero matches
3. `cd frontend && grep -c "var(--success)" components/ProductReview.tsx components/TopPickBlock.tsx components/ProductCards.tsx` — shows counts for each file (2+2+1 = at least 5 total)
4. `cd frontend && grep -c "var(--error)" components/ProductReview.tsx components/TopPickBlock.tsx components/ProductCards.tsx` — shows counts for each file (2+1+1 = at least 4 total)
5. `cd frontend && npm run test:run -- tests/designTokens.test.ts` — 13/13 pass
6. `cd frontend && npm run test:run` — full suite does not introduce NEW failures beyond the 21 pre-existing ones
</verification>

<success_criteria>
- Zero hardcoded Tailwind palette color classes (text-green-*, text-red-*, text-emerald-*) remain in the three target components
- Zero dark: utility prefixes remain in the three target components
- All sentiment labels use text-[var(--success)] or text-[var(--error)]
- No other parts of the components were modified (CTA buttons, images, prices, ratings untouched)
- Message.tsx and BlockRegistry.tsx were NOT opened or modified
- designTokens.test.ts still passes 13/13
- Full test suite does not increase failure count beyond 21 pre-existing
</success_criteria>

<output>
After completion, create `.planning/phases/17-token-foundation-dark-mode-fixes/17-02-SUMMARY.md`
</output>
