---
phase: 23-qa-remediation-unified-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/globals.css
  - frontend/app/not-found.tsx
  - frontend/components/ChatContainer.tsx
  - frontend/app/chat/page.tsx
  - frontend/tests/chatScreen.test.tsx
  - frontend/tests/notFound.test.tsx
  - frontend/tests/layout.test.tsx
autonomous: true
requirements:
  - QAR-08
  - QAR-09
  - QAR-10
  - QAR-11

must_haves:
  truths:
    - "Chat bubbles render at full readable width on mobile viewports (not collapsed to 167px)"
    - "Chat input is not hidden behind the nav bar on mobile (no z-index overlap)"
    - "Body uses overflow-x: clip (not hidden) so iOS zoom-pan works and sticky elements are not broken"
    - "Stop generating button renders correctly in dark mode using CSS variables"
    - "Invalid URLs show a custom editorial 404 page instead of raw Next.js default"
  artifacts:
    - path: "frontend/app/globals.css"
      provides: "overflow-x: clip on body"
      contains: "overflow-x: clip"
    - path: "frontend/app/not-found.tsx"
      provides: "Custom 404 page with editorial luxury theme"
      contains: "404"
    - path: "frontend/components/ChatContainer.tsx"
      provides: "Fixed z-index for chat input footer, overflow-clip on chat container"
      contains: "overflow-clip"
    - path: "frontend/app/chat/page.tsx"
      provides: "overflow-clip on ancestor containers"
      contains: "overflow-clip"
  key_links:
    - from: "frontend/app/globals.css"
      to: "body element"
      via: "overflow-x: clip replaces hidden"
      pattern: "overflow-x:\\s*clip"
    - from: "frontend/app/not-found.tsx"
      to: "Next.js App Router"
      via: "default export renders 404 page"
      pattern: "export default function NotFound"
---

<objective>
Fix five frontend P0/P1 bugs: (1) chat bubble width on mobile, (2) chat input hidden behind nav bar, (3) overflow-x: hidden -> clip on body and 4 chat ancestor containers, (4) stop button dark mode styling, (5) create custom 404 page.

Purpose: P0 bugs (bubble width, nav overlap) directly break the mobile chat experience. P1 bugs (overflow, dark mode, 404) are accessibility and polish issues visible to every user.
Output: Fixed CSS, new not-found.tsx, passing frontend tests.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-qa-remediation-unified-bug-fixes/23-RESEARCH.md

<interfaces>
<!-- Key code locations verified in RESEARCH.md -->

From frontend/components/Message.tsx (READ-ONLY — do not modify structure):
- Line 152: User bubble with `max-w-[85%]` and `style={{ minWidth: 'fit-content' }}`
- The `minWidth: 'fit-content'` combined with narrow parent causes 167px collapse on mobile

From frontend/components/ChatContainer.tsx:
- Line 756: `overflow-hidden` on chat-container div (change to overflow-clip)
- Lines 876-908: Chat input footer bar — no explicit position/z-index/bottom set
- Lines 889-896: Stop generating button — uses CSS vars already, verify dark theme values exist

From frontend/app/chat/page.tsx:
- Line 130: `overflow-hidden` on h-dvh wrapper
- Line 133: `overflow-hidden` on flex-1 inner
- Line 140: `overflow-hidden` on main

From frontend/app/globals.css:
- Line 274: `overflow-x: hidden` on body (change to clip)

Stop button classes: `bg-[var(--surface-elevated)] text-[var(--text-secondary)] hover:text-[var(--text)] hover:bg-[var(--surface-hover)]`
Verify these vars exist in [data-theme="dark"] block in globals.css.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for mobile bubble width, overflow-x, stop button dark mode, and 404 page</name>
  <files>frontend/tests/chatScreen.test.tsx, frontend/tests/layout.test.tsx, frontend/tests/notFound.test.tsx</files>
  <behavior>
    - chatScreen.test.tsx: User bubble container does NOT have `minWidth: 'fit-content'` inline style (or has a mobile-safe alternative). Stop button uses only CSS variable colors (no hardcoded hex values).
    - layout.test.tsx: Body CSS rule uses `overflow-x: clip` (not `hidden`). Verify by reading globals.css content.
    - notFound.test.tsx: NotFound component renders with "404" text, a "Back to home" link pointing to "/", and uses editorial theme classes (font-serif, var(--text), var(--primary)).
  </behavior>
  <action>
    Read existing test files to understand patterns:
    - `frontend/tests/chatScreen.test.tsx` — extend with stop button CSS var test
    - `frontend/tests/layout.test.tsx` — extend or create with overflow-x test
    - `frontend/tests/notFound.test.tsx` — create new

    For chatScreen.test.tsx: Add a test that renders the stop button and verifies all color classes use `var(--...)` syntax (no hardcoded hex or Tailwind color palette classes like `text-red-500`).

    For layout.test.tsx: Add a test that reads globals.css content and asserts `overflow-x: clip` appears in the body rule (not `overflow-x: hidden`). Use a simple string/regex check on the CSS file content.

    For notFound.test.tsx: Create new file. Render the NotFound component, assert "404" text is present, assert a link to "/" exists with text like "Back to home", assert editorial theme classes are used.

    Follow existing Vitest + @testing-library/react patterns. Mock framer-motion as done in other test files.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run tests/chatScreen.test.tsx tests/layout.test.tsx tests/notFound.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>New test functions exist and fail with clear assertion errors (RED state for new tests). Existing tests not broken.</done>
</task>

<task type="auto">
  <name>Task 2: Fix mobile bubble, nav overlap, overflow-x, stop button dark mode, and create 404 page</name>
  <files>frontend/components/ChatContainer.tsx, frontend/app/chat/page.tsx, frontend/app/globals.css, frontend/app/not-found.tsx</files>
  <action>
    Apply 5 fixes across 4 files:

    **Fix 1 — Chat bubble mobile width (QAR-08):**
    Read `frontend/components/Message.tsx` line 152. The issue is `style={{ minWidth: 'fit-content' }}` on the user bubble div. This causes narrow rendering when the parent is constrained on mobile. IMPORTANT: CONTEXT.md says "Never touch Message.tsx" for structure, but this is a CSS-only fix on the user bubble div, not a structural change.
    - Remove `style={{ minWidth: 'fit-content' }}` from the user bubble div
    - Alternatively, if Message.tsx truly cannot be touched, fix the parent container width in `ChatContainer.tsx` — ensure the `#message-container` wrapper uses `overflow-clip` instead of `overflow-hidden` (which creates a BFC that crushes flex children). The research confirms at line 119 that `overflow-hidden max-w-full` on `#message-container` is the root cause.
    - Read ChatContainer.tsx to find `#message-container` and change `overflow-hidden` to `overflow-clip`.

    **Fix 2 — Chat input nav overlap (QAR-08):**
    Read `frontend/components/ChatContainer.tsx` lines 876-908 (chat input footer bar). Add `position: sticky`, `bottom: 0`, `z-index: 40` to the footer bar's style or className. First check the mobile nav bar's z-index in `NavLayout.tsx` or `MobileHeader.tsx` to ensure the chat input sits above it.

    **Fix 3 — overflow-x: clip (QAR-09):**
    In `frontend/app/globals.css` around line 274, change `overflow-x: hidden` to `overflow-x: clip` in the `body` rule.
    
    In `frontend/app/chat/page.tsx`:
    - Line 130: change `overflow-hidden` to `overflow-clip` on h-dvh wrapper
    - Line 133: change `overflow-hidden` to `overflow-clip` on flex-1 inner
    - Line 140: change `overflow-hidden` to `overflow-clip` on main

    In `frontend/components/ChatContainer.tsx`:
    - Line 756: change `overflow-hidden` to `overflow-clip` on chat-container div

    **Fix 4 — Stop button dark mode (QAR-11):**
    Read `frontend/app/globals.css` and check that `--surface-elevated`, `--border`, `--text-secondary`, `--text`, and `--surface-hover` all have values inside the `[data-theme="dark"]` block. If any are missing, add them with appropriate dark mode values. The stop button already uses CSS vars (confirmed in RESEARCH.md) — the fix is ensuring the dark theme definitions exist.

    **Fix 5 — Custom 404 page (QAR-10):**
    Create `frontend/app/not-found.tsx` with the editorial luxury theme:
    ```tsx
    import Link from 'next/link'

    export default function NotFound() {
      return (
        <div className="flex flex-col items-center justify-center min-h-[60vh] px-4 text-center">
          <h1 className="font-serif text-5xl font-normal text-[var(--text)] mb-4">404</h1>
          <p className="text-[var(--text-secondary)] text-lg mb-8">
            That page doesn&apos;t exist.
          </p>
          <Link
            href="/"
            className="px-6 py-3 rounded-full bg-[var(--primary)] text-white text-sm font-medium hover:bg-[var(--primary-hover)] transition-colors"
          >
            Back to home
          </Link>
        </div>
      )
    }
    ```

    After all fixes, run the frontend test suite.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run tests/chatScreen.test.tsx tests/layout.test.tsx tests/notFound.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>All 5 fixes applied. Chat bubbles render at readable width on mobile. Chat input sits above nav bar. Body uses overflow-x: clip. Stop button dark mode CSS vars confirmed. Custom 404 page exists. All new and existing tests pass.</done>
</task>

</tasks>

<verification>
All frontend tests pass: `cd frontend && npm run test -- --run`
Build passes: `cd frontend && npm run build 2>&1 | tail -20`
</verification>

<success_criteria>
- User chat bubbles are not collapsed to 167px on mobile (parent overflow fixed)
- Chat input footer has z-index above mobile nav bar
- Body and 4 chat ancestor containers use overflow-clip instead of overflow-hidden
- Stop button dark mode CSS variables confirmed present in [data-theme="dark"]
- /nonexistent-page renders the custom editorial 404 page
- All frontend tests pass, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-qa-remediation-unified-bug-fixes/23-02-SUMMARY.md`
</output>
