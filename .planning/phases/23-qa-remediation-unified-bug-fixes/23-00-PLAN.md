---
phase: 23-qa-remediation-unified-bug-fixes
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/23-qa-remediation-unified-bug-fixes/BASELINE.md
autonomous: true
requirements:
  - QAR-00

must_haves:
  truths:
    - "A reproducible baseline snapshot exists before any bug fixes are applied"
    - "The baseline records the exact commit SHA, env config, model config, affiliate tag, and API base URL"
    - "8 canonical test prompts are documented with expected tool invocations"
  artifacts:
    - path: ".planning/phases/23-qa-remediation-unified-bug-fixes/BASELINE.md"
      provides: "Frozen pre-fix baseline: commit SHA, env snapshot, model config, affiliate tag, API URL, 8 canonical prompts"
      contains: "commit_sha"
  key_links: []
---

<objective>
Create a reproducible baseline snapshot before any QA remediation fixes are applied. This is the "before" measurement — all fixes will be measured against it.

Purpose: CONTEXT.md locked decision "Phase 0: Reproducible Baseline" requires recording the current state before making changes. Without this, there is no way to verify that fixes actually improved behavior.
Output: BASELINE.md with commit SHA, environment snapshot, model config, affiliate tag values, API base URL, and 8 canonical test prompts.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-qa-remediation-unified-bug-fixes/23-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BASELINE.md with pre-fix snapshot</name>
  <files>.planning/phases/23-qa-remediation-unified-bug-fixes/BASELINE.md</files>
  <action>
    Create BASELINE.md capturing the frozen pre-fix state:

    **Step 1 — Record commit SHA:**
    Run `git rev-parse HEAD` and record the result.

    **Step 2 — Record environment snapshot (redacted):**
    Read `docker-compose.yml` and `backend/.env` (or `backend/.env.example` if .env is gitignored). Record:
    - CORS_ORIGINS setting
    - SEARCH_PROVIDER value
    - RATE_LIMIT_ENABLED value
    - DATABASE_URL (redact password)
    - REDIS_URL (redact password if any)
    - Any feature flags (ENABLE_SERPAPI, etc.)
    Do NOT record actual secret keys — only record which env vars are set and their non-secret values.

    **Step 3 — Record model/provider config:**
    Read `backend/app/core/config.py` and record:
    - Default LLM model name (GPT-4o, etc.)
    - Search provider (openai/perplexity)
    - Any model temperature or token limit settings

    **Step 4 — Record affiliate tag values:**
    Search for affiliate tags in the codebase: `revguide-20` (Amazon), any eBay campid, Skimlinks publisher ID. Record current values.

    **Step 5 — Record API base URL:**
    From docker-compose.yml or frontend config, record the backend API URL (e.g., http://localhost:8000).

    **Step 6 — Document 8 canonical test prompts:**
    Create a table of 8 prompts that cover the key QAR behaviors:
    1. "best laptops for students" — tests product cards, accessory suppression (QAR-01, QAR-04)
    2. "best laptops under $500" — tests budget enforcement (QAR-05)
    3. "best wireless headphones" — tests fallback loop, multi-provider (QAR-01, QAR-02)
    4. "compare AirPods Pro vs Sony WH-1000XM5" — tests citations, label parity (QAR-03, QAR-07)
    5. "plan a 3-day trip to Paris" — tests travel timeout recovery (QAR-06)
    6. "best running shoes" — tests product cards general flow
    7. "hotels in Tokyo for next week" — tests travel partial response
    8. "best coffee makers under $200" — tests budget + product pipeline end-to-end

    For each prompt, list: prompt text, expected tools invoked, expected QAR behaviors exercised.

    **Step 7 — Write the file:**
    Structure BASELINE.md as:
    ```markdown
    # Phase 23 — Pre-Fix Baseline
    **Captured:** {date}
    **Commit SHA:** {sha}
    ## Environment Snapshot
    ...
    ## Model/Provider Config
    ...
    ## Affiliate Tags
    ...
    ## API Base URL
    ...
    ## Canonical Test Prompts
    | # | Prompt | Expected Tools | QAR Coverage |
    ...
    ```
  </action>
  <verify>
    <automated>test -f ".planning/phases/23-qa-remediation-unified-bug-fixes/BASELINE.md" && grep -c "commit_sha\|Commit SHA" ".planning/phases/23-qa-remediation-unified-bug-fixes/BASELINE.md"</automated>
  </verify>
  <done>BASELINE.md exists with commit SHA, redacted env snapshot, model config, affiliate tag values, API base URL, and 8 canonical test prompts with expected tool invocations and QAR coverage mapping.</done>
</task>

</tasks>

<verification>
BASELINE.md exists: `test -f .planning/phases/23-qa-remediation-unified-bug-fixes/BASELINE.md`
Contains all required sections: `grep -c "Environment\|Model\|Affiliate\|Canonical" .planning/phases/23-qa-remediation-unified-bug-fixes/BASELINE.md`
</verification>

<success_criteria>
- BASELINE.md exists with all 6 required sections
- Commit SHA is recorded (exact hash, not "HEAD")
- Environment snapshot redacts secrets but records which vars are set
- 8 canonical test prompts are documented with QAR coverage mapping
- No actual API keys or passwords appear in the file
</success_criteria>

<output>
After completion, create `.planning/phases/23-qa-remediation-unified-bug-fixes/23-00-SUMMARY.md`
</output>
