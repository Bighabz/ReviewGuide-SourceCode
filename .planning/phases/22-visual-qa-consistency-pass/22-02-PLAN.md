---
phase: 22-visual-qa-consistency-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/AffiliateLinks.tsx
  - frontend/components/ComparisonTable.tsx
  - frontend/components/ListBlock.tsx
  - frontend/components/ProductCarousel.tsx
  - frontend/components/PriceComparison.tsx
  - frontend/components/StarRating.tsx
  - frontend/components/browse/SentimentBar.tsx
  - frontend/components/SourceCitations.tsx
  - frontend/components/DestinationInfo.tsx
  - frontend/components/browse/CategoryNav.tsx
  - frontend/components/InlineProductCard.tsx
  - frontend/tests/sourceCitations.test.tsx
autonomous: true
requirements:
  - QA-03

must_haves:
  truths:
    - "A grep for hardcoded Tailwind palette utilities in v3.0-modified components returns zero results"
    - "The phantom --text-primary token is replaced with --text everywhere"
    - "All inert dark: Tailwind prefixes are removed from v3.0 components"
    - "Dark mode colors render correctly via CSS variables (no reliance on dark: prefix)"
  artifacts:
    - path: "frontend/components/AffiliateLinks.tsx"
      provides: "Price deal and best-price badge using CSS variables"
    - path: "frontend/components/ComparisonTable.tsx"
      provides: "Star ratings, best-price badges, pros/cons icons using CSS variables"
    - path: "frontend/components/ListBlock.tsx"
      provides: "Info/warning/tip list blocks using CSS variables"
    - path: "frontend/components/ProductCarousel.tsx"
      provides: "Discount badge and price using CSS variables"
    - path: "frontend/components/PriceComparison.tsx"
      provides: "Price deal indicators using CSS variables"
    - path: "frontend/components/StarRating.tsx"
      provides: "Star color using --rating-star CSS variable"
    - path: "frontend/components/browse/SentimentBar.tsx"
      provides: "Positive/negative bars using CSS variables"
    - path: "frontend/components/SourceCitations.tsx"
      provides: "Source dots using CSS variables + updated test assertions"
    - path: "frontend/components/DestinationInfo.tsx"
      provides: "Icon containers using CSS variables"
    - path: "frontend/components/browse/CategoryNav.tsx"
      provides: "Promo text and button using CSS variables"
    - path: "frontend/components/InlineProductCard.tsx"
      provides: "Product name using --text (not phantom --text-primary)"
  key_links:
    - from: "frontend/components/AffiliateLinks.tsx"
      to: "frontend/app/globals.css"
      via: "CSS variable references"
      pattern: "var\\(--price-deal\\)|var\\(--bold-green\\)|var\\(--success-light\\)"
    - from: "frontend/components/StarRating.tsx"
      to: "frontend/app/globals.css"
      via: "CSS variable reference"
      pattern: "var\\(--rating-star\\)"
    - from: "frontend/components/InlineProductCard.tsx"
      to: "frontend/app/globals.css"
      via: "CSS variable reference"
      pattern: "var\\(--text\\)"
---

<objective>
Replace all hardcoded Tailwind color utilities in v3.0-modified components with CSS variable references, remove inert dark: prefixes, and fix the phantom --text-primary token.

Purpose: QA-03 requires zero hardcoded Tailwind palette utilities in any component modified during v3.0. The CSS variable system defined in Phase 17 provides semantic replacements for every hardcoded color. This also fixes the phantom --text-primary token (undefined, silently falls back to browser default black) in two components.
Output: 11 component files updated, 1 test file updated, grep audit returns zero matches.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-visual-qa-consistency-pass/22-RESEARCH.md

@frontend/app/globals.css
@frontend/components/AffiliateLinks.tsx
@frontend/components/ComparisonTable.tsx
@frontend/components/ListBlock.tsx
@frontend/components/ProductCarousel.tsx
@frontend/components/PriceComparison.tsx
@frontend/components/StarRating.tsx
@frontend/components/browse/SentimentBar.tsx
@frontend/components/SourceCitations.tsx
@frontend/components/DestinationInfo.tsx
@frontend/components/browse/CategoryNav.tsx
@frontend/components/InlineProductCard.tsx
@frontend/tests/sourceCitations.test.tsx

<interfaces>
<!-- CSS variable replacement map from 22-RESEARCH.md -->
<!-- The executor MUST use these exact token names -->

Replacement map (hardcoded -> CSS variable):
```
text-green-600 / text-emerald-600  -> style={{ color: 'var(--price-deal)' }} or text-[var(--bold-green)]
bg-green-100 / bg-green-500/5      -> style={{ backgroundColor: 'var(--success-light)' }}
bg-green-500/10 / bg-emerald-500/10 -> style={{ backgroundColor: 'var(--success-light)' }}
bg-emerald-500                     -> style={{ backgroundColor: 'var(--bold-green)' }}
text-emerald-600                   -> style={{ color: 'var(--bold-green)' }}
text-red-500 / text-red-600        -> style={{ color: 'var(--bold-red)' }} or text-[var(--bold-red)]
bg-red-100                         -> style={{ backgroundColor: 'color-mix(in srgb, var(--error) 10%, transparent)' }}
text-blue-600                      -> style={{ color: 'var(--bold-blue)' }}
bg-blue-100                        -> style={{ backgroundColor: 'color-mix(in srgb, var(--bold-blue) 10%, transparent)' }}
text-blue-200                      -> style={{ color: 'var(--text-muted)' }}
bg-blue-50                         -> style={{ backgroundColor: 'var(--surface-hover)' }}
text-yellow-400 / text-yellow-500  -> style={{ color: 'var(--rating-star)' }}
fill-yellow-400                    -> style={{ fill: 'var(--rating-star)', color: 'var(--rating-star)' }}
text-orange-500                    -> style={{ color: 'var(--bold-amber)' }}
bg-red-500 (dots)                  -> style={{ backgroundColor: 'var(--bold-red)' }}
bg-blue-500 (dots)                 -> style={{ backgroundColor: 'var(--bold-blue)' }}
bg-green-500 (dots)                -> style={{ backgroundColor: 'var(--bold-green)' }}
bg-orange-500 (dots)               -> style={{ backgroundColor: 'var(--bold-amber)' }}
border-green-500/30                -> style={{ borderColor: 'color-mix(in srgb, var(--bold-green) 30%, transparent)' }}

PHANTOM TOKEN FIX:
var(--text-primary)                -> var(--text)
```

CRITICAL RULES:
1. All `dark:*` Tailwind variants are INERT (data-theme strategy). Remove them alongside the light mode hardcoded class.
2. When replacing Tailwind classes with inline styles, use `style={{}}` syntax.
3. When using Tailwind arbitrary value syntax, use `text-[var(--token)]` or `bg-[var(--token)]`.
4. ErrorBoundary.tsx is OUT OF SCOPE (not modified in v3.0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded colors in AffiliateLinks, ComparisonTable, ListBlock, ProductCarousel, PriceComparison</name>
  <files>
    frontend/components/AffiliateLinks.tsx,
    frontend/components/ComparisonTable.tsx,
    frontend/components/ListBlock.tsx,
    frontend/components/ProductCarousel.tsx,
    frontend/components/PriceComparison.tsx
  </files>
  <action>
Replace every hardcoded Tailwind color utility with the corresponding CSS variable. Remove all inert `dark:` variants. Use the replacement map from the interfaces block.

**AffiliateLinks.tsx:**
- Line 53: `border-green-500/30 bg-green-500/5 hover:bg-green-500/10` -> use inline style with `borderColor: 'color-mix(in srgb, var(--bold-green) 30%, transparent)'`, `backgroundColor: 'var(--success-light)'`. For hover, use a conditional or CSS variable approach.
- Line 59: `text-green-600 bg-green-100 ... dark:text-green-400 dark:bg-green-900/30` -> replace entire className segment with inline style: `style={{ color: 'var(--price-deal)', backgroundColor: 'var(--success-light)' }}`. Remove `dark:*` classes.
- Line 68: `text-green-600 dark:text-green-400` -> replace with `style={{ color: 'var(--price-deal)' }}` when `isLowest` is true. Keep the existing `text-[var(--text)]` for the else case.

**ComparisonTable.tsx:**
- Line 52: `fill-yellow-400 text-yellow-400` -> replace with `style={{ color: 'var(--rating-star)', fill: 'var(--rating-star)' }}` and remove the Tailwind color classes.
- Line 140: `text-emerald-600 dark:text-emerald-400` -> replace with `style={{ color: 'var(--bold-green)' }}` when `isBest`.
- Line 144: `text-emerald-600 ... bg-emerald-100 dark:bg-emerald-900/30` -> replace with inline styles using `var(--bold-green)` for text and `var(--success-light)` for background. Remove `dark:*`.
- Line 172: `text-emerald-600 ... bg-emerald-100 dark:bg-emerald-900/30 dark:text-emerald-400` -> same pattern.
- Line 216: `text-emerald-500` -> replace with `style={{ color: 'var(--bold-green)' }}`.
- Line 247: `text-red-500` -> replace with `style={{ color: 'var(--bold-red)' }}`.

**ListBlock.tsx:**
- Lines 21-23 (info variant): `bg-blue-100 dark:bg-blue-900/30` / `text-blue-600 dark:text-blue-400` / `bg-blue-500` -> These are in a config object. Replace with CSS variable values:
  - `bg: 'bg-[color-mix(in_srgb,var(--bold-blue)_10%,transparent)]'` or switch to inline styles. The cleanest approach: change the variant config to use style objects instead of className strings. For each variant, define `bgStyle`, `textStyle`, `bulletStyle` as CSS properties referencing tokens.
- Lines 31-33 (warning variant): `bg-red-100 dark:bg-red-900/30` / `text-red-600 dark:text-red-400` / `bg-red-500` -> same pattern with `--bold-red` / `--error`.
- If other variants exist with hardcoded colors, fix those too.

For ListBlock: if refactoring to style objects is too invasive, use Tailwind arbitrary value syntax: `bg-[var(--success-light)]`, `text-[var(--bold-blue)]`, etc. This keeps the className-based approach while using CSS variables.

**ProductCarousel.tsx:**
- Line 183: `bg-emerald-500/10 text-emerald-600 dark:bg-emerald-400/15 dark:text-emerald-400` -> replace with inline styles: `style={{ backgroundColor: 'var(--success-light)', color: 'var(--bold-green)' }}`. Remove `dark:*` classes.
- Line 213: `text-emerald-600 dark:text-emerald-400` -> replace with `style={{ color: 'var(--bold-green)' }}`.

**PriceComparison.tsx:**
- Line 58: `text-emerald-600 dark:text-emerald-400` -> `style={{ color: 'var(--bold-green)' }}`
- Line 59: same pattern
- Line 92: `bg-emerald-100 text-emerald-700 dark:bg-emerald-900/40 dark:text-emerald-300` -> inline style with `var(--success-light)` bg and `var(--bold-green)` text
- Line 101: `text-emerald-600 dark:text-emerald-400` -> `style={{ color: 'var(--bold-green)' }}`

After each file is updated, verify no hardcoded Tailwind color classes remain in that file:
```bash
grep -n 'text-green-\|text-red-\|text-emerald-\|bg-blue-\|text-blue-\|bg-green-\|bg-red-\|bg-emerald-\|text-yellow-\|fill-yellow-\|dark:text-\|dark:bg-' frontend/components/AffiliateLinks.tsx frontend/components/ComparisonTable.tsx frontend/components/ListBlock.tsx frontend/components/ProductCarousel.tsx frontend/components/PriceComparison.tsx
```
  </action>
  <verify>
    <automated>cd frontend && grep -rn 'text-green-\|text-red-\|text-emerald-\|bg-blue-\|text-blue-\|bg-green-\|bg-red-\|bg-emerald-\|text-yellow-\|fill-yellow-\|dark:text-\|dark:bg-' components/AffiliateLinks.tsx components/ComparisonTable.tsx components/ListBlock.tsx components/ProductCarousel.tsx components/PriceComparison.tsx; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero hardcoded Tailwind color utilities in AffiliateLinks.tsx, ComparisonTable.tsx, ListBlock.tsx, ProductCarousel.tsx, PriceComparison.tsx. All inert dark: variants removed. CSS variables used for all semantic colors.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded colors in StarRating, SentimentBar, SourceCitations, DestinationInfo, CategoryNav + fix phantom --text-primary</name>
  <files>
    frontend/components/StarRating.tsx,
    frontend/components/browse/SentimentBar.tsx,
    frontend/components/SourceCitations.tsx,
    frontend/components/DestinationInfo.tsx,
    frontend/components/browse/CategoryNav.tsx,
    frontend/components/InlineProductCard.tsx,
    frontend/tests/sourceCitations.test.tsx
  </files>
  <action>
Replace remaining hardcoded Tailwind color utilities and fix the phantom `--text-primary` token.

**StarRating.tsx:**
- Line 28: `text-yellow-500` -> remove class, add `style={{ color: 'var(--rating-star)' }}`
- Line 36: `text-yellow-500` -> remove class, add `style={{ color: 'var(--rating-star)' }}`. Keep `fill="currentColor"` as-is (it inherits from the color style).

**SentimentBar.tsx (frontend/components/browse/SentimentBar.tsx):**
- Line 24: `className="bg-emerald-500"` -> `style={{ backgroundColor: 'var(--bold-green)' }}` (remove className for color)
- Line 37: `className="text-emerald-600"` -> `style={{ color: 'var(--bold-green)' }}`

**SourceCitations.tsx:**
- Line 29: `DOT_BG_CLASSES = ['bg-red-500', 'bg-blue-500', 'bg-green-500', 'bg-orange-500']` -> Replace with CSS variable inline styles. Change the array to style objects:
  ```typescript
  const DOT_COLORS = [
    'var(--bold-red)',
    'var(--bold-blue)',
    'var(--bold-green)',
    'var(--bold-amber)',
  ]
  ```
  Then where dots are rendered, instead of `className={DOT_BG_CLASSES[i]}`, use `style={{ backgroundColor: DOT_COLORS[i % DOT_COLORS.length] }}`.

- Line 80: `style={{ color: 'var(--text-primary)' }}` -> Fix phantom token: change to `style={{ color: 'var(--text)' }}`

- **Update sourceCitations.test.tsx:** The test queries for `[class*="bg-red-"]`, `[class*="bg-blue-"]`, `[class*="bg-green-"]`. Since dots now use inline styles instead of classes, update test assertions to query by `[style*="--bold-red"]`, `[style*="--bold-blue"]`, `[style*="--bold-green"]` respectively. Or use a more robust query like checking that dot elements have a `style` attribute containing `backgroundColor`.

**DestinationInfo.tsx:**
- Line 23: `bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400` -> `style={{ backgroundColor: 'color-mix(in srgb, var(--bold-blue) 10%, transparent)', color: 'var(--bold-blue)' }}`. Remove `dark:*` classes.
- Line 61: `bg-emerald-100 dark:bg-emerald-900/30 text-emerald-600 dark:text-emerald-400` -> `style={{ backgroundColor: 'var(--success-light)', color: 'var(--bold-green)' }}`. Remove `dark:*`.
- Line 71: `text-emerald-500` -> `style={{ color: 'var(--bold-green)' }}`

**CategoryNav.tsx (frontend/components/browse/CategoryNav.tsx):**
- Line 57: `text-blue-200` -> `style={{ color: 'var(--text-muted)' }}`
- Line 59: `bg-white text-[var(--secondary)] ... hover:bg-blue-50` -> Replace `bg-white` with `style={{ backgroundColor: 'var(--background)' }}` and `hover:bg-blue-50` with `hover:bg-[var(--surface-hover)]`. Keep `text-[var(--secondary)]` as-is (already using CSS variable).

**InlineProductCard.tsx:**
- Line 112: `style={{ color: 'var(--text-primary)' }}` -> Fix phantom token: change to `style={{ color: 'var(--text)' }}`

After updating each file, verify:
```bash
grep -n 'text-green-\|text-red-\|text-emerald-\|bg-blue-\|text-blue-\|bg-green-\|bg-red-\|bg-emerald-\|text-yellow-\|fill-yellow-\|bg-orange-\|dark:text-\|dark:bg-\|text-primary' frontend/components/StarRating.tsx frontend/components/browse/SentimentBar.tsx frontend/components/SourceCitations.tsx frontend/components/DestinationInfo.tsx frontend/components/browse/CategoryNav.tsx frontend/components/InlineProductCard.tsx
```

Run tests to confirm no regressions:
```bash
cd frontend && npm run test:run
```
  </action>
  <verify>
    <automated>cd frontend && grep -rn 'text-green-\|text-red-\|text-emerald-\|bg-blue-\|text-blue-\|bg-green-\|bg-red-\|bg-emerald-\|text-yellow-\|fill-yellow-\|bg-orange-\|dark:text-\|dark:bg-' components/StarRating.tsx components/browse/SentimentBar.tsx components/SourceCitations.tsx components/DestinationInfo.tsx components/browse/CategoryNav.tsx components/InlineProductCard.tsx; echo "EXIT:$?" && grep -rn 'text-primary' components/InlineProductCard.tsx components/SourceCitations.tsx; echo "PHANTOM:$?"</automated>
  </verify>
  <done>Zero hardcoded Tailwind color utilities in StarRating, SentimentBar, SourceCitations, DestinationInfo, CategoryNav, and InlineProductCard. Phantom --text-primary replaced with --text in InlineProductCard.tsx and SourceCitations.tsx. SourceCitations test updated to match new inline style dot pattern. Full test suite still passes.</done>
</task>

</tasks>

<verification>
Run the QA-03 success criterion grep across ALL v3.0-modified components:
```bash
cd frontend && grep -rn 'text-green-\|text-red-\|text-emerald-\|bg-blue-\|text-blue-\|bg-green-\|bg-red-\|bg-emerald-\|text-yellow-\|fill-yellow-\|bg-orange-\|dark:text-\|dark:bg-' components/AffiliateLinks.tsx components/ComparisonTable.tsx components/ListBlock.tsx components/ProductCarousel.tsx components/PriceComparison.tsx components/StarRating.tsx components/browse/SentimentBar.tsx components/SourceCitations.tsx components/DestinationInfo.tsx components/browse/CategoryNav.tsx components/InlineProductCard.tsx
```
Expected: Zero results.

Verify phantom token is gone:
```bash
cd frontend && grep -rn '\-\-text-primary' components/ --include='*.tsx'
```
Expected: Zero results.

Full test suite:
```bash
cd frontend && npm run test:run 2>&1 | tail -5
```
Expected: Zero failures.
</verification>

<success_criteria>
1. `grep -rn 'text-green-\|text-red-\|...' components/{all 11 files}` returns zero matches
2. `grep -rn '\-\-text-primary' components/` returns zero matches
3. `npm run test:run` passes with zero failures
4. All 11 component files use CSS variable references instead of hardcoded Tailwind colors
5. All inert `dark:` Tailwind variants removed from v3.0 components
</success_criteria>

<output>
After completion, create `.planning/phases/22-visual-qa-consistency-pass/22-02-SUMMARY.md`
</output>
