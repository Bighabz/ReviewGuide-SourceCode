---
phase: 13-discover-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/discoverScreen.test.tsx
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05

must_haves:
  truths:
    - "Test file exists with 10 behavioral contracts covering all 5 DISC requirements"
    - "All tests fail (RED) because production components do not yet exist"
    - "Tests use existing setup.ts mocks (router, localStorage) — no new test infrastructure"
  artifacts:
    - path: "frontend/tests/discoverScreen.test.tsx"
      provides: "Behavioral contracts for DiscoverPage, DiscoverSearchBar, CategoryChipRow, TrendingCards"
      min_lines: 80
  key_links:
    - from: "frontend/tests/discoverScreen.test.tsx"
      to: "frontend/app/page.tsx"
      via: "import DiscoverPage (default export)"
      pattern: "import.*page"
---

<objective>
Write Wave 0 RED test scaffolds for the Discover screen — 10 behavioral contracts covering DISC-01 through DISC-05.

Purpose: Establish testable contracts before any production code is written, ensuring all Discover screen behaviors are verified automatically.
Output: `frontend/tests/discoverScreen.test.tsx` with 10 failing tests.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-discover-screen/13-CONTEXT.md
@.planning/phases/13-discover-screen/13-RESEARCH.md
@.planning/phases/13-discover-screen/13-VALIDATION.md

@frontend/tests/setup.ts
@frontend/tests/navLayout.test.tsx
@frontend/tests/mobileTabBar.test.tsx

<interfaces>
<!-- Existing test setup the executor needs to know about -->

From frontend/tests/setup.ts:
```typescript
// Already mocked: useRouter (push, replace, prefetch, back), usePathname (returns '/'), localStorage (getItem, setItem, removeItem, clear), fetch, CSS variables, scrollIntoView
import '@testing-library/jest-dom'
import { vi } from 'vitest'

vi.mock('next/navigation', () => ({
  useRouter: () => ({
    push: vi.fn(),
    replace: vi.fn(),
    prefetch: vi.fn(),
    back: vi.fn(),
  }),
  usePathname: () => '/',
  useSearchParams: () => new URLSearchParams(),
}))

const localStorageMock = {
  getItem: vi.fn(),
  setItem: vi.fn(),
  removeItem: vi.fn(),
  clear: vi.fn(),
}
Object.defineProperty(window, 'localStorage', { value: localStorageMock })
```

From frontend/lib/recentSearches.ts:
```typescript
export interface RecentSearch {
  query: string
  productNames: string[]
  category: string
  timestamp: number
}
export function getRecentSearches(): RecentSearch[]
```

From frontend/lib/categoryConfig.ts:
```typescript
export interface BrowseCategory {
  slug: string
  name: string
  tagline: string
  image: string
  icon: string
  queries: string[]
}
export const categories: BrowseCategory[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write 10 failing behavioral test contracts for Discover screen</name>
  <files>frontend/tests/discoverScreen.test.tsx</files>
  <behavior>
    DISC-01 (Hero):
    - Test 1: `/` renders heading containing "researching" in italic span
    - Test 2: `/` renders subline "Expert reviews, real data, zero fluff."

    DISC-02 (Category Chips):
    - Test 3: Category chip row renders at least 8 chips (Popular, Tech, Travel, Kitchen, Fitness, Home, Fashion, Outdoor)
    - Test 4: Tapping a category chip calls router.push with `/chat?q=...&new=1` URL

    DISC-03 (Trending Cards):
    - Test 5: At least 3 trending cards render with title and subtitle text
    - Test 6: Tapping a trending card calls router.push with encoded query parameter

    DISC-04 (For You):
    - Test 7: "For You" chip is NOT rendered when localStorage has no recent searches
    - Test 8: "For You" chip IS rendered when localStorage returns recent searches

    DISC-05 (Search Bar):
    - Test 9: Clicking search bar calls router.push('/chat?new=1')
    - Test 10: Search bar renders as a button element (not input or textarea)
  </behavior>
  <action>
    Create `frontend/tests/discoverScreen.test.tsx` with 10 tests organized in describe blocks by requirement ID.

    Key patterns to follow (from existing Phase 12 tests):
    - Import render/screen/fireEvent from @testing-library/react
    - Import vi from vitest
    - Mock `next/navigation` per-test where needed (use `vi.mocked()` pattern from mobileTabBar.test.tsx)
    - For DISC-04 tests, mock `@/lib/recentSearches` module — return empty array for "no history" test, non-empty array for "has history" test
    - The tests will import from `@/app/page` (the DiscoverPage component) — this import will fail until Plan 02 creates the component, making all tests RED
    - Use `screen.getByRole('button')`, `screen.getByText()`, `screen.getAllByRole()` assertions
    - For router.push assertions: import useRouter from the mock, then check `vi.mocked(useRouter().push)` after fireEvent.click
    - Do NOT use `waitFor` or async patterns — these are synchronous render tests

    IMPORTANT: The existing `setup.ts` mocks `useRouter` globally. For tests that need to assert `router.push` was called, you need to capture the mock BEFORE the click:
    ```typescript
    import { useRouter } from 'next/navigation'
    // In test:
    const push = vi.fn()
    vi.mocked(useRouter).mockReturnValue({ push, replace: vi.fn(), prefetch: vi.fn(), back: vi.fn() } as any)
    ```

    After writing, run the tests — they MUST all fail (component not found / import error). This confirms RED state.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/discoverScreen.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>10 tests exist in discoverScreen.test.tsx. All 10 fail because the DiscoverPage component does not yet exist (RED state confirmed). No new test infrastructure or setup.ts modifications needed.</done>
</task>

</tasks>

<verification>
- `frontend/tests/discoverScreen.test.tsx` exists with 10 test cases
- Running `cd frontend && npx vitest run tests/discoverScreen.test.tsx` shows all 10 tests FAILING
- No modifications to `frontend/tests/setup.ts`
</verification>

<success_criteria>
All 10 behavioral contracts written and confirmed RED. Test file covers every DISC requirement at least once.
</success_criteria>

<output>
After completion, create `.planning/phases/13-discover-screen/13-01-SUMMARY.md`
</output>
