---
phase: 13-discover-screen
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - frontend/lib/trendingTopics.ts
  - frontend/components/discover/DiscoverSearchBar.tsx
  - frontend/components/discover/CategoryChipRow.tsx
  - frontend/components/discover/TrendingCards.tsx
  - frontend/app/page.tsx
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05

must_haves:
  truths:
    - "User lands on `/` and sees editorial hero headline with serif italic 'researching' and a centered search bar"
    - "User can scroll a horizontal row of 8 category chips and tap one to navigate to chat"
    - "User sees at least 3 trending research cards with icons, titles, subtitles"
    - "User with recent search history sees 'For You' as the first chip"
    - "Tapping the search bar navigates to `/chat?new=1` without accepting typing"
  artifacts:
    - path: "frontend/lib/trendingTopics.ts"
      provides: "TrendingTopic interface and static array of 6 trending topics"
      exports: ["TrendingTopic", "trendingTopics"]
    - path: "frontend/components/discover/DiscoverSearchBar.tsx"
      provides: "Visual CTA search bar (button, not input) that navigates to /chat?new=1"
      min_lines: 15
    - path: "frontend/components/discover/CategoryChipRow.tsx"
      provides: "Horizontal scrollable chip row with 8 categories + optional For You chip"
      min_lines: 40
    - path: "frontend/components/discover/TrendingCards.tsx"
      provides: "Trending topic card list with Lucide icons and navigation"
      min_lines: 30
    - path: "frontend/app/page.tsx"
      provides: "DiscoverPage orchestrator rendering hero + chips + trending"
      min_lines: 20
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/components/discover/DiscoverSearchBar.tsx"
      via: "import and render"
      pattern: "import.*DiscoverSearchBar"
    - from: "frontend/app/page.tsx"
      to: "frontend/components/discover/CategoryChipRow.tsx"
      via: "import and render with hasHistory prop"
      pattern: "import.*CategoryChipRow"
    - from: "frontend/app/page.tsx"
      to: "frontend/components/discover/TrendingCards.tsx"
      via: "import and render"
      pattern: "import.*TrendingCards"
    - from: "frontend/components/discover/CategoryChipRow.tsx"
      to: "frontend/lib/categoryConfig.ts"
      via: "import categories for chip data"
      pattern: "import.*categoryConfig"
    - from: "frontend/components/discover/TrendingCards.tsx"
      to: "frontend/lib/trendingTopics.ts"
      via: "import trendingTopics array"
      pattern: "import.*trendingTopics"
    - from: "frontend/app/page.tsx"
      to: "frontend/lib/recentSearches.ts"
      via: "useEffect guard for localStorage read"
      pattern: "getRecentSearches"
---

<objective>
Build the complete Discover screen: data file, three sub-components, and the page orchestrator at `/`. All 10 behavioral tests from Plan 01 must turn GREEN.

Purpose: Replace the single-line redirect at `app/page.tsx` with a full editorial Discover screen — the unified entry point for the app.
Output: 5 new/modified files constituting the Discover screen.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-discover-screen/13-CONTEXT.md
@.planning/phases/13-discover-screen/13-RESEARCH.md

@frontend/app/browse/page.tsx
@frontend/lib/categoryConfig.ts
@frontend/lib/recentSearches.ts
@frontend/lib/utils.ts
@frontend/app/globals.css
@frontend/tests/discoverScreen.test.tsx

<interfaces>
<!-- Existing code contracts the executor needs -->

From frontend/lib/categoryConfig.ts:
```typescript
export interface BrowseCategory {
  slug: string
  name: string
  tagline: string
  image: string
  icon: string  // lucide-react icon name
  queries: string[]
}
export const categories: BrowseCategory[]
// Categories available: travel, electronics, home-appliances, health-wellness,
// outdoor-fitness, fashion-style, smart-home, kids-toys, baby, big-tall
```

From frontend/lib/recentSearches.ts:
```typescript
export interface RecentSearch {
  query: string
  productNames: string[]
  category: string
  timestamp: number
}
export function getRecentSearches(): RecentSearch[]
```

From frontend/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From frontend/app/globals.css:
```css
/* Already exists — no need to add */
.scrollbar-hide {
  -ms-overflow-style: none;
  scrollbar-width: none;
}
.scrollbar-hide::-webkit-scrollbar {
  display: none;
}
```

CSS Variables available (use these, NOT Tailwind dark: utilities):
- `var(--text)`, `var(--text-secondary)`, `var(--text-muted)`
- `var(--primary)` (#1B4DFF)
- `var(--background)`, `var(--surface)`, `var(--surface-elevated)`, `var(--surface-hover)`
- `var(--border)`
- `var(--shadow-sm)`

Tailwind classes available:
- `font-serif` maps to Instrument Serif (editorial headings)
- `product-card-hover` for card lift effect
- `shadow-card` for card elevation
- `.scrollbar-hide` for hidden scrollbar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create trendingTopics.ts data file and three Discover sub-components</name>
  <files>
    frontend/lib/trendingTopics.ts
    frontend/components/discover/DiscoverSearchBar.tsx
    frontend/components/discover/CategoryChipRow.tsx
    frontend/components/discover/TrendingCards.tsx
  </files>
  <action>
    **1. Create `frontend/lib/trendingTopics.ts`:**

    Define `TrendingTopic` interface with: `id`, `title`, `subtitle`, `query`, `icon` (Lucide icon name string), `iconBg` (pastel hex), `iconColor` (icon stroke hex). Export a `trendingTopics` array with 6 items:

    | title | subtitle | icon | iconBg | iconColor |
    |-------|----------|------|--------|-----------|
    | Best Headphones 2026 | Noise-cancelling picks at every price | Headphones | #EFF6FF | #3B82F6 |
    | Tokyo Travel Guide | Flights, stays and hidden gems | Plane | #FFF7ED | #F97316 |
    | Top Laptops for Students | Performance meets portability | Laptop2 | #F0FDF4 | #22C55E |
    | Best Robot Vacuums | Hands-free cleaning, tested | Bot | #FDF4FF | #A855F7 |
    | Running Shoes Ranked | From trail to treadmill | Footprints | #FEF2F2 | #EF4444 |
    | Smart Home Starter Kit | Automate your home under $200 | Speaker | #ECFDF5 | #10B981 |

    **2. Create `frontend/components/discover/DiscoverSearchBar.tsx`:**

    `'use client'` component. Renders a `<button>` (NOT input/textarea) styled to look like a search bar. Per user decisions:
    - Height 56px, 1px solid var(--border), 16px border-radius, subtle shadow via `var(--shadow-sm, 0 1px 3px rgba(0,0,0,0.06))`
    - Left magnifying glass icon (Search from lucide-react, size 18)
    - Placeholder text: "Ask anything — best headphones, Tokyo trip, laptop deals..."
    - Text color: var(--text-muted), text size: 14px (text-sm)
    - Background: var(--surface-elevated)
    - On click: `router.push('/chat?new=1')`
    - Hover: `border-[var(--primary)]/40` transition
    - `aria-label="Start a research session"`

    **3. Create `frontend/components/discover/CategoryChipRow.tsx`:**

    `'use client'` component. Props: `{ hasHistory: boolean }`.

    Build a static chip config array (NOT iterating `categories` directly — display names differ):
    ```
    Popular -> navigates to /chat?new=1
    Tech -> electronics slug -> query: "Best noise-cancelling headphones"
    Travel -> travel slug -> query: "Top all-inclusive resorts in the Caribbean"
    Kitchen -> home-appliances slug -> query: "Best robot vacuums for pet hair"
    Fitness -> outdoor-fitness slug -> query: "Best hiking boots for beginners"
    Home -> smart-home slug -> query: "Best Alexa-compatible smart home gadgets"
    Fashion -> fashion-style slug -> query: "Best white sneakers for everyday wear"
    Outdoor -> outdoor-fitness slug -> query: "Best hiking boots for beginners"
    ```

    If `hasHistory` is true, prepend a "For You" chip as the FIRST chip (before Popular).

    Chip styling per user decisions:
    - Horizontal scrollable row, no wrap: `flex gap-2 overflow-x-auto scrollbar-hide`
    - Chip height 36px, padding 12px 16px, corner radius 20px (pill shape)
    - Active chip state is NOT needed for this phase — all chips just navigate on tap
    - Inactive chips: transparent bg, `border border-[var(--border)]`, `text-[var(--text)]`
    - Font: DM Sans (default body font) SemiBold 13px
    - On click: `router.push('/chat?q=<query>&new=1')` (use encodeURIComponent)
    - "For You" chip: same styling, navigates to `/chat?new=1` (no specific query)

    When "For You" is present and active, show recent searches inline below the chip row. Use the existing recent-search card pattern from `browse/page.tsx` lines 72-88: a `flex gap-3 overflow-x-auto` row of 220px cards with query text and product names. For this first pass, "For You" chip simply navigates to `/chat?new=1` without inline expansion (Claude's discretion — inline expansion can be enhanced later).

    **4. Create `frontend/components/discover/TrendingCards.tsx`:**

    `'use client'` component. Imports `trendingTopics` from `@/lib/trendingTopics`.

    Section header: "TRENDING RESEARCH" — uppercase, DM Sans Medium 11px, var(--text-muted), letter-spacing 1.5px (tracking-[1.5px]).

    Each card (~72px height, horizontal layout):
    - Left: 40x40 circle with pastel background (`iconBg`) + Lucide icon (dynamic icon resolution)
    - Center: title (DM Sans SemiBold 15px, var(--text)) + subtitle (DM Sans Regular 13px, var(--text-secondary))
    - Right: chevron `>` (ChevronRight from lucide-react, size 16, var(--text-muted))
    - `product-card-hover` class for lift effect
    - On click: `router.push('/chat?q=<topic.query>&new=1')` with encodeURIComponent
    - Cards are a vertical stack on mobile (3+ visible without scrolling)
    - Desktop: 2-column grid via `grid grid-cols-1 md:grid-cols-2 gap-3`
    - Bottom arrow "See more topics" link-like text with `->` arrow hint, navigating to `/chat?new=1`

    For dynamic Lucide icon rendering: import the specific icons used in trendingTopics (Headphones, Plane, Laptop2, Bot, Footprints, Speaker) and create a simple icon map object. Do NOT use `lucide-react/dynamicIconImports` (adds bundle weight). Example:
    ```typescript
    import { Headphones, Plane, Laptop2, Bot, Footprints, Speaker } from 'lucide-react'
    const iconMap: Record<string, any> = { Headphones, Plane, Laptop2, Bot, Footprints, Speaker }
    ```
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/discoverScreen.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 files created. trendingTopics.ts exports TrendingTopic interface and 6 topics. DiscoverSearchBar is a button (not input). CategoryChipRow renders 8+ chips with "For You" conditional on hasHistory. TrendingCards renders 6 cards with Lucide icons.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DiscoverPage orchestrator in app/page.tsx</name>
  <files>frontend/app/page.tsx</files>
  <action>
    Replace the entire content of `frontend/app/page.tsx` (currently `import { redirect } from 'next/navigation'; export default function Home() { redirect('/browse'); }`).

    New content — a `'use client'` orchestrator component:

    ```
    'use client'

    import { useState, useEffect } from 'react'
    import { getRecentSearches } from '@/lib/recentSearches'
    import DiscoverSearchBar from '@/components/discover/DiscoverSearchBar'
    import CategoryChipRow from '@/components/discover/CategoryChipRow'
    import TrendingCards from '@/components/discover/TrendingCards'
    ```

    Component `DiscoverPage`:
    - `const [hasHistory, setHasHistory] = useState(false)`
    - `useEffect(() => { setHasHistory(getRecentSearches().length > 0) }, [])`
    - Returns a `<div className="flex flex-col pb-20 px-4 sm:px-6 md:px-8">` containing:
      1. Hero section: `<div className="flex flex-col items-center pt-8 sm:pt-12 pb-8">` with:
         - `<h1>` using `font-serif text-2xl sm:text-3xl md:text-4xl text-center text-[var(--text)] leading-tight tracking-tight`
         - Text: "What are you " + `<span className="italic text-[var(--primary)]">researching</span>` + " today?"
         - `<p>` subline: "Expert reviews, real data, zero fluff." — `text-sm text-[var(--text-secondary)] text-center mt-3 max-w-md`
         - Search bar: `<div className="w-full max-w-xl mx-auto mt-8"><DiscoverSearchBar /></div>`
      2. Category chips: `<div className="mt-8"><CategoryChipRow hasHistory={hasHistory} /></div>`
      3. Trending section: `<div className="mt-10"><TrendingCards /></div>`

    CRITICAL: Do NOT render any topbar/header — NavLayout in layout.tsx already provides it.
    CRITICAL: Do NOT add the animated video logo — user decision: "Drop the animated video logo — clean editorial hero only."
    CRITICAL: Do NOT use `<input>` or `<textarea>` — the search bar is imported as DiscoverSearchBar (a button).

    After saving, run the test suite to confirm all 10 tests go GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/discoverScreen.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>app/page.tsx is a 'use client' DiscoverPage component (no redirect). All 10 behavioral tests from Plan 01 pass GREEN. Hero shows "researching" in italic serif, search bar is a button, 8+ category chips render, 3+ trending cards render, "For You" chip respects localStorage state.</done>
</task>

</tasks>

<verification>
- All 10 tests in `discoverScreen.test.tsx` pass GREEN: `cd frontend && npx vitest run tests/discoverScreen.test.tsx`
- Full test suite does not regress: `cd frontend && npx vitest run`
- No Tailwind `dark:` utilities used in new components (grep check)
- No `<input>` or `<textarea>` in DiscoverSearchBar (grep check)
</verification>

<success_criteria>
Discover screen renders at `/` with editorial hero, 8 category chips, 6 trending cards, conditional "For You" chip, and a tap-to-navigate search bar. All 10 automated tests GREEN. No regressions in existing test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/13-discover-screen/13-02-SUMMARY.md`
</output>
