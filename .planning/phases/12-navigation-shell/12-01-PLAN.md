---
phase: 12-navigation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/navLayout.test.tsx
  - frontend/tests/mobileTabBar.test.tsx
  - frontend/tests/pageTransition.test.tsx
  - frontend/tests/layout.test.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05]

must_haves:
  truths:
    - "Test files exist for all 5 NAV requirements before any production code is written"
    - "Tests assert the correct behaviors: tab count, FAB navigation, safe area style, viewport-fit, transition animation"
    - "All tests fail (RED) because production components do not yet exist"
  artifacts:
    - path: "frontend/tests/navLayout.test.tsx"
      provides: "Tests for NavLayout responsive behavior, route exclusion, desktop topbar rendering"
      min_lines: 40
    - path: "frontend/tests/mobileTabBar.test.tsx"
      provides: "Tests for 5 tabs, FAB router.push, active tab highlighting, safe area padding, keyboard hide"
      min_lines: 60
    - path: "frontend/tests/pageTransition.test.tsx"
      provides: "Tests for template.tsx motion.div with opacity animation"
      min_lines: 15
    - path: "frontend/tests/layout.test.tsx"
      provides: "Tests for viewport viewportFit: 'cover' in layout.tsx export"
      min_lines: 10
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for all Phase 12 NAV requirements.

Purpose: Establish RED tests before any production code so that subsequent plans have automated verification at every step. Tests define the behavioral contract for NavLayout, MobileTabBar, MobileHeader, and template.tsx.
Output: 4 test files in frontend/tests/ that fail because the components they import do not yet exist.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-navigation-shell/12-CONTEXT.md
@.planning/phases/12-navigation-shell/12-RESEARCH.md
@.planning/phases/12-navigation-shell/12-VALIDATION.md

<interfaces>
<!-- Existing test setup that all new test files should use -->

From frontend/tests/setup.ts:
```typescript
import '@testing-library/jest-dom'
import { vi } from 'vitest'

vi.mock('next/navigation', () => ({
  useRouter: () => ({
    push: vi.fn(),
    replace: vi.fn(),
    prefetch: vi.fn(),
    back: vi.fn(),
  }),
  usePathname: () => '/',
  useSearchParams: () => new URLSearchParams(),
}))

const localStorageMock = {
  getItem: vi.fn(),
  setItem: vi.fn(),
  removeItem: vi.fn(),
  clear: vi.fn(),
}
Object.defineProperty(window, 'localStorage', { value: localStorageMock })
global.fetch = vi.fn()
Element.prototype.scrollIntoView = vi.fn()
```

From frontend/vitest.config.ts:
```typescript
test: {
  environment: 'jsdom',
  globals: true,
  setupFiles: ['./tests/setup.ts'],
  include: ['**/*.{test,spec}.{js,mjs,cjs,ts,mts,cts,jsx,tsx}'],
}
resolve: {
  alias: { '@': path.resolve(__dirname, './') }
}
```

From frontend/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffolds for NavLayout and MobileTabBar</name>
  <files>frontend/tests/navLayout.test.tsx, frontend/tests/mobileTabBar.test.tsx</files>
  <action>
Create two test files that define the behavioral contracts for the navigation shell:

**navLayout.test.tsx** — Tests for the NavLayout component (to be created at `frontend/components/NavLayout.tsx`):
1. "renders MobileTabBar on mobile viewport" — render NavLayout, assert that MobileTabBar content is present (look for tab labels: Discover, Saved, Ask, Compare, Profile)
2. "renders UnifiedTopbar on desktop viewport" — mock a desktop context, assert UnifiedTopbar is rendered (check for the logo or nav links)
3. "does not render tab bar or header for excluded routes" — mock usePathname to return '/admin/dashboard', render NavLayout, assert no tab bar labels appear
4. "does not render tab bar for /privacy route" — mock usePathname to '/privacy', assert no tab labels
5. "renders Footer only on desktop" — assert Footer content appears in a desktop context but not on mobile

Since these tests import components that don't exist yet, they WILL fail. That's correct — they establish the RED phase.

Mock `usePathname` per-test by re-mocking `next/navigation` at the top of the file with a configurable return value (use `vi.mocked()` or a module-level variable that individual tests set). The global setup.ts mock returns '/' by default, which is fine for most tests. For route-specific tests, use `vi.mock` with a factory that returns the desired pathname.

Use `@testing-library/react` render and screen queries. Import components from `@/components/NavLayout`, `@/components/MobileTabBar` etc.

**mobileTabBar.test.tsx** — Tests for MobileTabBar (to be at `frontend/components/MobileTabBar.tsx`):
1. "renders exactly 5 tab items" — render MobileTabBar, query all buttons with tab labels, expect 5
2. "renders Discover, Saved, Ask, Compare, Profile labels" — assert each label text is present
3. "FAB button calls router.push('/chat?new=1') on click" — find the Ask/FAB button, fireEvent.click, assert router.push was called with '/chat?new=1'
4. "active tab shows blue color for current route" — mock pathname to '/browse', render, assert Discover tab has active styling
5. "tab bar has safe area padding style" — render MobileTabBar, assert the nav element has paddingBottom that includes 'env(safe-area-inset-bottom)' in its inline style
6. "tab bar hides when keyboard is open" — mock window.visualViewport to simulate keyboard open state (height < 75% of innerHeight), assert the nav element is not rendered

To test router.push for FAB: override the global useRouter mock in this test file to capture calls. Use `vi.fn()` for push and assert against it.

For the visualViewport test: define `window.visualViewport` as a mock object with `height`, `addEventListener`, and `removeEventListener`. Trigger the resize handler to simulate keyboard open.

All tests should import from paths that will exist after Plan 02 creates the components. Tests are expected to FAIL at this stage.
  </action>
  <verify>
    <automated>cd /c/Users/habib/downloads/reviewguide-sourcecode/frontend && npx vitest run tests/navLayout.test.tsx tests/mobileTabBar.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist with all described test cases. Tests fail because the imported components do not exist yet (import errors or "Cannot find module"). This is the expected RED state.</done>
</task>

<task type="auto">
  <name>Task 2: Create test scaffolds for page transitions and layout viewport</name>
  <files>frontend/tests/pageTransition.test.tsx, frontend/tests/layout.test.tsx</files>
  <action>
Create two test files for the remaining NAV requirements:

**pageTransition.test.tsx** — Tests for template.tsx (to be at `frontend/app/template.tsx`):
1. "template.tsx renders children inside a motion.div" — import Template from `@/app/template`, render with a child element, assert child is rendered
2. "motion.div has initial opacity 0 and animate opacity 1" — render Template, check that the wrapping div has the expected framer-motion animation props (use `data-testid` or check for the motion div's role/structure). Since Framer Motion renders actual DOM elements, assert the child content is visible.

This test validates NAV-04 (animated page transitions).

**layout.test.tsx** — Tests for viewport-fit=cover in layout.tsx:
1. "viewport export includes viewportFit: 'cover'" — This is a static export test. Import the `viewport` named export from `@/app/layout` and assert `viewport.viewportFit === 'cover'`. Note: layout.tsx is a Server Component, so we may need to import just the exported constant. If direct import fails due to React Server Component restrictions, use the file-read approach: read `frontend/app/layout.tsx` as a string and assert it contains `viewportFit: 'cover'` (same pattern as designTokens.test.ts in the existing test suite).

This test validates NAV-05 (iOS safe area prerequisite).

Both files should follow the existing test patterns: `describe`/`it` blocks, `@testing-library/react` for rendering, `vi` for mocking.
  </action>
  <verify>
    <automated>cd /c/Users/habib/downloads/reviewguide-sourcecode/frontend && npx vitest run tests/pageTransition.test.tsx tests/layout.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist. pageTransition.test.tsx fails because template.tsx does not exist yet (expected RED). layout.test.tsx may partially pass if using file-read approach (viewportFit not yet added), or fail on import. Either way, the test contract is established.</done>
</task>

</tasks>

<verification>
All 4 test files exist in frontend/tests/. Running `cd frontend && npx vitest run tests/navLayout.test.tsx tests/mobileTabBar.test.tsx tests/pageTransition.test.tsx tests/layout.test.tsx` shows test failures (RED state) because production components don't exist yet.
</verification>

<success_criteria>
- 4 test files created covering all 5 NAV requirements
- Tests define behavioral contracts matching CONTEXT.md decisions
- Tests are structured to turn GREEN when Plan 02 and Plan 03 create the production components
- No production code created in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-shell/12-01-SUMMARY.md`
</output>
