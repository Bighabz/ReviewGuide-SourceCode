---
phase: 16-placeholder-routes-and-build-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/saved/page.tsx
  - frontend/app/compare/page.tsx
  - frontend/components/MobileTabBar.tsx
  - frontend/components/UnifiedTopbar.tsx
autonomous: true
requirements:
  - PLCH-01
  - PLCH-02

must_haves:
  truths:
    - "Navigating to /saved shows a polished placeholder page with Coming Soon message"
    - "Navigating to /compare shows a polished placeholder page with Coming Soon message"
    - "MobileTabBar Saved tab links to /saved (not /browse)"
    - "MobileTabBar Compare tab links to /compare (not /browse)"
    - "UnifiedTopbar Saved link points to /saved (not /browse)"
    - "UnifiedTopbar Compare link points to /compare (not /browse)"
    - "next build completes with zero errors"
    - "All existing tests pass"
  artifacts:
    - path: "frontend/app/saved/page.tsx"
      provides: "Static placeholder page for /saved route"
      contains: "Coming soon"
    - path: "frontend/app/compare/page.tsx"
      provides: "Static placeholder page for /compare route"
      contains: "Coming soon"
    - path: "frontend/components/MobileTabBar.tsx"
      provides: "Updated Saved/Compare tab hrefs"
      contains: "/saved"
    - path: "frontend/components/UnifiedTopbar.tsx"
      provides: "Updated Saved/Compare nav link hrefs"
      contains: "/saved"
  key_links:
    - from: "frontend/components/MobileTabBar.tsx"
      to: "frontend/app/saved/page.tsx"
      via: "TABS href: '/saved'"
      pattern: "href.*'/saved'"
    - from: "frontend/components/MobileTabBar.tsx"
      to: "frontend/app/compare/page.tsx"
      via: "TABS href: '/compare'"
      pattern: "href.*'/compare'"
    - from: "frontend/components/UnifiedTopbar.tsx"
      to: "frontend/app/saved/page.tsx"
      via: "Link href='/saved'"
      pattern: "href.*[\"']/saved"
    - from: "frontend/components/UnifiedTopbar.tsx"
      to: "frontend/app/compare/page.tsx"
      via: "Link href='/compare'"
      pattern: "href.*[\"']/compare"
---

<objective>
Create polished editorial placeholder pages for /saved and /compare routes, update all navigation components to link to real routes instead of /browse fallback, and validate the entire v2.0 milestone builds cleanly.

Purpose: This is the final quality gate for milestone v2.0. All bottom tab destinations must resolve to working pages, and `next build` must pass with zero errors to confirm production deployability.
Output: Two new route pages, updated navigation hrefs, clean build output.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-placeholder-routes-and-build-qa/16-CONTEXT.md

@frontend/components/MobileTabBar.tsx
@frontend/components/UnifiedTopbar.tsx
@frontend/app/globals.css
</context>

<interfaces>
<!-- Key contracts the executor needs -->

From frontend/components/MobileTabBar.tsx (line 16-22):
```typescript
const TABS = [
  { id: 'discover', label: 'Discover', icon: Home, href: '/' },
  { id: 'saved', label: 'Saved', icon: Bookmark, href: '/browse' },    // UPDATE to '/saved'
  { id: 'ask', label: 'Ask', icon: Plus, href: null },
  { id: 'compare', label: 'Compare', icon: LayoutGrid, href: '/browse' }, // UPDATE to '/compare'
  { id: 'profile', label: 'Profile', icon: User, href: null },
] as const
```

From frontend/components/UnifiedTopbar.tsx (lines 150-176):
```typescript
// Saved link (line 151): href="/browse"  → UPDATE to href="/saved"
// Compare link (line 169): href="/browse" → UPDATE to href="/compare"
```

From frontend/components/MobileTabBar.tsx active tab detection (lines 116-127):
```typescript
const getIsActive = (tabId: string, href: string | null) => {
  if (tabId === 'discover') {
    return pathname?.startsWith('/browse') || pathname === '/'
  }
  if (tabId === 'ask') {
    return pathname?.startsWith('/chat') || pathname?.startsWith('/results')
  }
  if (href) {
    return pathname?.startsWith(href)  // This already works for /saved and /compare
  }
  return false
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder pages and update navigation hrefs</name>
  <files>frontend/app/saved/page.tsx, frontend/app/compare/page.tsx, frontend/components/MobileTabBar.tsx, frontend/components/UnifiedTopbar.tsx</files>
  <action>
**Create `frontend/app/saved/page.tsx`** — static Server Component (NO 'use client'):
- Centered layout using flexbox with `min-h-[60vh]` so it fills the viewport within NavLayout
- Lucide `Bookmark` icon (size 48, strokeWidth 1.5) with `text-[var(--text-muted)]` color
- Heading: `<h1 className="font-serif italic text-2xl" style={{ color: 'var(--text)' }}>Saved Items</h1>`
- Subtext: `<p style={{ color: 'var(--text-secondary)' }}>This feature is coming soon. We're building something great.</p>`
- CTA: `<Link href="/">` styled as a button with `bg-[var(--primary)] text-white rounded-lg px-6 py-2.5 text-sm font-medium` — label "Back to Discover"
- All styling via CSS variables (var(--*)), no Tailwind dark: utilities (per Phase 12 locked decision)
- Import `Link` from `next/link`, `Bookmark` from `lucide-react`

**Create `frontend/app/compare/page.tsx`** — identical structure to saved/page.tsx except:
- Lucide `BarChart3` icon instead of `Bookmark`
- Heading: "Compare Products" instead of "Saved Items"
- Import `BarChart3` from `lucide-react`

**Update `frontend/components/MobileTabBar.tsx`** — change TABS array:
- Line 18: `{ id: 'saved', label: 'Saved', icon: Bookmark, href: '/saved' }` (was '/browse')
- Line 20: `{ id: 'compare', label: 'Compare', icon: LayoutGrid, href: '/compare' }` (was '/browse')
- The `getIsActive` function already handles arbitrary hrefs via `pathname?.startsWith(href)` so no changes needed there

**Update `frontend/components/UnifiedTopbar.tsx`** — change nav Link hrefs:
- Saved link (around line 151): change `href="/browse"` to `href="/saved"`
- Compare link (around line 169): change `href="/browse"` to `href="/compare"`
- The `getActiveTab` function already has `pathname?.startsWith('/saved')` and `pathname?.startsWith('/compare')` cases (lines 47-48) so active state detection works automatically
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
- /saved renders a centered editorial placeholder with Bookmark icon, "Saved Items" heading, coming soon text, and Back to Discover CTA
- /compare renders same layout with BarChart3 icon and "Compare Products" heading
- MobileTabBar Saved tab navigates to /saved, Compare tab navigates to /compare
- UnifiedTopbar Saved link points to /saved, Compare link points to /compare
- All existing tests still pass (no regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Build QA — verify next build and full test suite</name>
  <files>frontend/app/saved/page.tsx, frontend/app/compare/page.tsx</files>
  <action>
**Run full build validation:**

1. Run `cd frontend && npm run build` — must complete with zero errors
2. Inspect build output to confirm:
   - `/saved` appears as a route (should be "(Static)" since it's a Server Component with no dynamic data)
   - `/compare` appears as a route (should be "(Static)" similarly)
   - `/` (Discover), `/chat`, `/results/[id]` all still appear correctly
   - No TypeScript errors, no ESLint warnings treated as errors
3. Run `cd frontend && npx vitest run` — all existing tests must pass
4. If build fails due to any issue in the new placeholder pages (e.g., missing import, SSR-incompatible code), fix immediately

**If either page is NOT marked "(Static)":** Remove any 'use client' directive or dynamic imports that might have been added. These pages have zero interactivity and must be static.

**Dark mode validation note:** The placeholder pages use only CSS variables (`var(--text)`, `var(--text-muted)`, `var(--text-secondary)`, `var(--primary)`, `var(--background)`) which are already defined for both light and dark themes in globals.css. No additional dark mode work needed — the CSS variable system handles it automatically.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
- `next build` completes with zero errors
- /saved and /compare appear in build output as routes (ideally Static)
- All existing test suites pass with no regressions
- No TypeScript compilation errors across the entire frontend
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx vitest run` — all tests pass
2. `cd frontend && npm run build` — zero errors, /saved and /compare routes present
3. Manual spot-check: navigate to localhost:3000/saved and /compare — both show editorial placeholder
4. MobileTabBar Saved/Compare tabs link to correct routes (grep TABS in MobileTabBar.tsx confirms '/saved' and '/compare')
5. UnifiedTopbar nav links updated (grep href in UnifiedTopbar.tsx confirms '/saved' and '/compare')
</verification>

<success_criteria>
- Both /saved and /compare render polished editorial placeholder pages with no errors
- MobileTabBar and UnifiedTopbar link to /saved and /compare instead of /browse
- `next build` passes cleanly with all routes present
- Full test suite green
- v2.0 milestone confirmed production-deployable
</success_criteria>

<output>
After completion, create `.planning/phases/16-placeholder-routes-and-build-qa/16-01-SUMMARY.md`
</output>
