---
phase: 19-mosaic-hero
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - frontend/app/page.tsx
autonomous: false
requirements:
  - HERO-02

must_haves:
  truths:
    - "Landing page hero section shows mosaic tiles behind the headline and search bar"
    - "Headline 'What are you researching today?' is legible over the mosaic background"
    - "Subline 'Expert reviews, real data, zero fluff.' is legible over the mosaic background"
    - "Gradient scrim sits between mosaic and text, using var(--mosaic-scrim) for theme awareness"
    - "Existing discoverScreen.test.tsx tests (DISC-01 through DISC-05) all pass unchanged"
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "MosaicHero wired into hero section with gradient scrim overlay"
      contains: "MosaicHero"
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/components/discover/MosaicHero.tsx"
      via: "import and JSX render"
      pattern: "import MosaicHero from"
    - from: "frontend/app/page.tsx"
      to: "frontend/app/globals.css"
      via: "var(--mosaic-scrim) CSS variable reference"
      pattern: "mosaic-scrim"
---

<objective>
Wire MosaicHero into the discover landing page with a gradient scrim for text readability.

Purpose: The mosaic background creates the first-impression visual richness users see when they land on the discover page. The gradient scrim ensures the headline and search bar float legibly over the image tiles. This completes the Shopify-style hero effect.

Output: Updated page.tsx with MosaicHero positioned behind hero text via z-index layering
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-mosaic-hero/19-RESEARCH.md
@.planning/phases/19-mosaic-hero/19-01-SUMMARY.md

@frontend/app/page.tsx
@frontend/components/discover/MosaicHero.tsx
@frontend/tests/discoverScreen.test.tsx

<interfaces>
<!-- From Plan 01 output -->

From frontend/components/discover/MosaicHero.tsx (created in Plan 01):
```tsx
// Default export, no props needed
export default function MosaicHero(): JSX.Element
// Renders 8 tilted/overlapping product image tiles
// aria-hidden="true", height: clamp(200px, 30vw, 320px)
```

From frontend/app/globals.css (added in Plan 01):
```css
/* :root */
--mosaic-scrim: linear-gradient(to bottom, rgba(250,250,247,0.45) 0%, rgba(250,250,247,0.80) 55%, #FAFAF7 100%);

/* [data-theme="dark"] */
--mosaic-scrim: linear-gradient(to bottom, rgba(10,12,18,0.45) 0%, rgba(10,12,18,0.80) 55%, #0A0C12 100%);
```

Current hero section structure in page.tsx (lines 26-43):
```tsx
<div className="flex flex-col items-center pt-2 sm:pt-8 pb-4">
  <h1 className="font-serif text-[28px] sm:text-4xl md:text-5xl ..." style={{ color: 'var(--text)' }}>
    What are you{' '}<span className="italic" style={{ color: 'var(--primary)' }}>researching</span>{' '}today?
  </h1>
  <p className="text-sm sm:text-[15px] ..." style={{ color: 'var(--text-secondary)' }}>
    Expert reviews, real data, zero fluff.
  </p>
</div>
```

Tests that MUST survive (from discoverScreen.test.tsx):
- DISC-01: Queries for italic span containing "researching" and subline text
- DISC-02 through DISC-05: Category chips, trending cards, search bar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire MosaicHero into page.tsx hero section</name>
  <files>frontend/app/page.tsx</files>
  <action>
Edit `frontend/app/page.tsx` to add MosaicHero as a background layer behind the existing hero headline text. This is a SURGICAL edit — the text content (h1, span, p) must remain exactly as-is to preserve existing test assertions.

**Step 1 — Add import:**

At the top of the file, after existing imports, add:
```tsx
import MosaicHero from '@/components/discover/MosaicHero'
```

**Step 2 — Wrap hero section with position context:**

Replace the existing hero section div (the `<div className="flex flex-col items-center pt-2 sm:pt-8 pb-4">` block containing the h1 and p) with this structure:

```tsx
{/* Hero section — mosaic background + floating text */}
<div className="relative flex flex-col items-center pt-2 sm:pt-8 pb-4 overflow-hidden rounded-2xl">
  {/* Mosaic sits behind everything — z-0 */}
  <div className="absolute inset-0 z-0">
    <MosaicHero />
  </div>

  {/* Gradient scrim for text readability — z-[1], above mosaic, below text */}
  <div
    className="absolute inset-0 z-[1]"
    style={{ background: 'var(--mosaic-scrim)' }}
  />

  {/* Text content floats on top — z-[2] */}
  <div className="relative z-[2] flex flex-col items-center">
    <h1
      className="font-serif text-[28px] sm:text-4xl md:text-5xl text-center leading-tight tracking-tight animate-fade-up"
      style={{ color: 'var(--text)' }}
    >
      What are you{' '}
      <span className="italic" style={{ color: 'var(--primary)' }}>
        researching
      </span>
      {' '}today?
    </h1>
    <p
      className="text-sm sm:text-[15px] text-center mt-3 max-w-md leading-relaxed"
      style={{ color: 'var(--text-secondary)' }}
    >
      Expert reviews, real data, zero fluff.
    </p>
  </div>
</div>
```

**Key z-index layers:**
- z-0: MosaicHero (background tiles)
- z-[1]: Gradient scrim using `var(--mosaic-scrim)` — theme-aware, no hardcoded colors
- z-[2]: Text content (h1, p) — floats on top with `position: relative`

**Critical constraints:**
- Do NOT modify the h1 text content, the italic span, or the p subline text — these are tested by discoverScreen.test.tsx DISC-01.
- The outer wrapper adds `relative` (for absolute positioning context), `overflow-hidden` (clips rotated tile edges at container boundary), and `rounded-2xl` (subtle rounding for visual polish).
- The scrim uses `var(--mosaic-scrim)` — NOT hardcoded rgba values. This ensures dark mode works correctly.
- All other sections of page.tsx (CategoryChipRow, ProductCarousel, DiscoverSearchBar) are left completely unchanged.

**Step 3 — Verify tests pass:**

Run discoverScreen.test.tsx to confirm the DOM restructuring preserves all DISC-01 through DISC-05 assertions. The italic span "researching" and the subline text must still be findable in the rendered output.

Run mosaicHero.test.tsx to confirm the MosaicHero component still renders correctly inside the page context.
  </action>
  <verify>
    <automated>cd frontend && npm run test:run -- --reporter=verbose tests/discoverScreen.test.tsx tests/mosaicHero.test.tsx</automated>
  </verify>
  <done>
    - page.tsx imports and renders MosaicHero inside the hero section
    - Mosaic sits at z-0, gradient scrim at z-[1], text content at z-[2]
    - Scrim uses var(--mosaic-scrim) for theme-aware gradient
    - h1 text and p subline are unchanged — DISC-01 tests pass
    - All discoverScreen.test.tsx and mosaicHero.test.tsx tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of mosaic hero on landing page</name>
  <files>frontend/app/page.tsx</files>
  <action>
Human verifies the mosaic hero renders correctly in the browser. The executor starts the dev server if not already running (`cd frontend && npm run dev`), then presents verification steps to the user.
  </action>
  <verify>
    <automated>cd frontend && npm run test:run -- --reporter=verbose tests/discoverScreen.test.tsx tests/mosaicHero.test.tsx</automated>
  </verify>
  <done>Human approves the visual result or describes issues to fix</done>
  <what-built>
    Mosaic hero collage on the discover landing page. 8 tilted, overlapping product image tiles sit behind the headline "What are you researching today?" with a gradient scrim for text readability. The mosaic is pure CSS (no JS library), theme-aware (dark mode scrim adjusts automatically), and optimized for Core Web Vitals (first tile eager-loaded, all tiles have explicit dimensions).
  </what-built>
  <how-to-verify>
    1. Start the frontend dev server: `cd frontend && npm run dev`
    2. Open http://localhost:3000 in Chrome
    3. Verify: Product image tiles are visible behind the headline text, tilted at various angles, overlapping slightly
    4. Verify: Headline "What are you researching today?" is clearly legible — the gradient scrim provides sufficient contrast
    5. Verify: On mobile (375px viewport via DevTools), only 4 center tiles show (outer tiles hidden)
    6. Verify: Scrolling down, the mosaic stays contained to the hero section — does not bleed into category chips or carousel
    7. Verify: Category chips, product carousel, and search bar below the hero are unaffected
    8. (Optional) Toggle dark mode in DevTools — scrim should adjust from ivory fade to navy-black fade
    9. (Optional) Run Lighthouse mobile audit — LCP should be under 2.5s, CLS under 0.1
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
# Discover screen tests (DISC-01 through DISC-05 must pass)
cd frontend && npm run test:run -- --reporter=verbose tests/discoverScreen.test.tsx

# MosaicHero component tests (HERO-01, HERO-03, HERO-04)
cd frontend && npm run test:run -- --reporter=verbose tests/mosaicHero.test.tsx

# Full test suite
cd frontend && npm run test:run
```
</verification>

<success_criteria>
- MosaicHero renders as a background layer behind the discover page hero text
- Headline and subline are legible over the mosaic (gradient scrim provides contrast)
- Dark mode scrim works correctly (no light band at bottom on dark backgrounds)
- DISC-01 through DISC-05 existing tests all pass
- HERO-01 through HERO-04 all addressed (mosaic visible, text readable, CSS-only, eager LCP)
- Human approves the visual result
</success_criteria>

<output>
After completion, create `.planning/phases/19-mosaic-hero/19-02-SUMMARY.md`
</output>
