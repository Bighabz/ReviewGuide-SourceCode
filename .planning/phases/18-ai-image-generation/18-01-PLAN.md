---
phase: 18-ai-image-generation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/imageAssets.test.ts
  - scripts/optimize-images.mjs
autonomous: true
requirements: [IMG-01, IMG-02, IMG-03]

must_haves:
  truths:
    - "Vitest test file exists that will assert 15+ category WebP files, 8+ mosaic WebP files, and all under 200KB"
    - "A Node.js optimization script exists that converts PNGs to WebP using sharp with correct quality/size parameters"
    - "The categories/ directory exists and is ready to receive generated images"
  artifacts:
    - path: "frontend/tests/imageAssets.test.ts"
      provides: "Filesystem-based assertions for IMG-01, IMG-02, IMG-03"
      contains: "describe.*IMG-01"
    - path: "scripts/optimize-images.mjs"
      provides: "Batch PNG-to-WebP conversion script using sharp"
      contains: "sharp"
  key_links:
    - from: "frontend/tests/imageAssets.test.ts"
      to: "frontend/public/images/categories/"
      via: "readdirSync"
      pattern: "readdirSync.*categories"
    - from: "frontend/tests/imageAssets.test.ts"
      to: "frontend/public/images/products/"
      via: "readdirSync"
      pattern: "mosaic-.*webp"
    - from: "scripts/optimize-images.mjs"
      to: "frontend/public/images/"
      via: "sharp pipeline"
      pattern: "sharp.*webp"
---

<objective>
Create the test scaffold and optimization tooling that subsequent image generation plans depend on.

Purpose: Establish automated verification (Nyquist Wave 0) so that after images are generated and optimized, a single `npm run test:run -- imageAssets` confirms all three IMG requirements are met. Also create the sharp-based batch conversion script that Plan 03 will run.
Output: `frontend/tests/imageAssets.test.ts`, `scripts/optimize-images.mjs`
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-ai-image-generation/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create imageAssets test scaffold</name>
  <files>frontend/tests/imageAssets.test.ts</files>
  <action>
Create `frontend/tests/imageAssets.test.ts` with three describe blocks covering all three IMG requirements:

**IMG-01 test:** Assert that `frontend/public/images/categories/` contains 15 or more files ending in `.webp`. Use `readdirSync` and `filter(f => f.endsWith('.webp'))`. Expected file names: `cat-headphones.webp`, `cat-laptops.webp`, `cat-kitchen.webp`, `cat-travel.webp`, `cat-fitness.webp`, `cat-smartphones.webp`, `cat-gaming.webp`, `cat-home-decor.webp`, `cat-beauty.webp`, `cat-outdoor.webp`, `cat-fashion.webp`, `cat-smart-home.webp`, `cat-audio.webp`, `cat-cameras.webp`, `cat-furniture.webp`.

**IMG-02 test:** Assert that `frontend/public/images/products/` contains 8 or more files matching `mosaic-*.webp`. Use `filter(f => f.startsWith('mosaic-') && f.endsWith('.webp'))`. Expected: `mosaic-headphones.webp`, `mosaic-laptop.webp`, `mosaic-sneakers.webp`, `mosaic-espresso.webp`, `mosaic-smartwatch.webp`, `mosaic-camera.webp`, `mosaic-fitness-gear.webp`, `mosaic-speaker.webp`.

**IMG-03 test:** For each `.webp` file in `categories/` and each `mosaic-*.webp` in `products/`, assert `statSync(filePath).size < 200_000`. Include the filename and actual KB in the error message for debuggability: `expect(size, '${f} is ${(size/1024).toFixed(1)}KB').toBeLessThan(200_000)`.

Use `path.resolve(__dirname, '../public/images')` as root. Import from `vitest` (`describe, it, expect`) and from `fs` (`readdirSync, statSync, existsSync`) and `path`.

Add a guard at the top of each describe that skips if the directory doesn't exist yet (`if (!existsSync(dir)) return` with a descriptive skip message), so the test doesn't crash before images are generated -- use `it.skip` pattern when directory is empty.

The test is expected to FAIL right now (no images exist). That's correct -- it validates after Plans 02 and 03 complete.
  </action>
  <verify>
    <automated>cd frontend && npm run test:run -- --reporter=verbose imageAssets 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists, vitest discovers it, and it reports skipped or failing tests (not errors/crashes). Three describe blocks for IMG-01, IMG-02, IMG-03 are present.</done>
</task>

<task type="auto">
  <name>Task 2: Create WebP batch optimization script and ensure directories exist</name>
  <files>scripts/optimize-images.mjs</files>
  <action>
1. Create directory `scripts/` at project root if it doesn't exist.

2. Create `scripts/optimize-images.mjs` that:
   - Imports `sharp` from the frontend's node_modules: use `import { createRequire } from 'module'; const require = createRequire(import.meta.url); const sharp = require('../frontend/node_modules/sharp');` -- OR simply `import sharp from 'sharp'` and run from `frontend/` directory. Use the createRequire approach so the script runs from project root.
   - Defines three target directories: `frontend/public/images/products`, `frontend/public/images/categories`, `frontend/public/images/topics`
   - Defines max width per directory: products=800, categories=1200, topics=800
   - For each directory, reads all `.png` files
   - For each PNG: resize to maxWidth (withoutEnlargement: true), convert to WebP (quality: 75, effort: 6), save as `.webp` alongside the PNG
   - After successful conversion, delete the source PNG
   - For any output WebP still over 200KB: re-run with quality: 60 and log a warning
   - Print a summary: filename, input size, output size, pass/fail vs 200KB limit
   - Handle errors gracefully: if a file fails, log error and continue to next file

3. Ensure `frontend/public/images/categories/` directory exists (create if needed via mkdir).

Do NOT run the script yet -- there are no new PNGs to convert. It will be invoked by Plan 03 after all images are generated.
  </action>
  <verify>
    <automated>node -c scripts/optimize-images.mjs && echo "Syntax OK"</automated>
  </verify>
  <done>Script file exists at `scripts/optimize-images.mjs`, passes Node.js syntax check, and `frontend/public/images/categories/` directory exists.</done>
</task>

</tasks>

<verification>
- `frontend/tests/imageAssets.test.ts` exists and vitest can discover it
- `scripts/optimize-images.mjs` exists and passes syntax check
- `frontend/public/images/categories/` directory exists
</verification>

<success_criteria>
- Test scaffold runs without crashes (tests skip or fail gracefully when no images present)
- Optimization script has valid syntax and correct sharp API usage
- Both files are committed and ready for Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/18-ai-image-generation/18-01-SUMMARY.md`
</output>
