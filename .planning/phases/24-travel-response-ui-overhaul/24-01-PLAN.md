---
phase: 24-travel-response-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/travelUI.test.tsx
  - frontend/components/ResortCards.tsx
  - frontend/components/blocks/BlockRegistry.tsx
autonomous: true
requirements: [TRV-01]

must_haves:
  truths:
    - "Attractions block renders as visual cards with hero images instead of a flat bullet list"
    - "Each resort card shows the resort name prominently with a destination image"
    - "Resort cards use a deterministic image lookup (no Math.random, no hydration errors)"
    - "Cards display in single-column on mobile, 2-column grid on sm+ (640px)"
    - "Fallback image is used when no name match exists in the lookup map"
  artifacts:
    - path: "frontend/components/ResortCards.tsx"
      provides: "Resort card grid component with image-mapped attraction cards"
      min_lines: 60
    - path: "frontend/tests/travelUI.test.tsx"
      provides: "Test scaffold for travel UI requirements TRVL-01 through TRVL-07"
      min_lines: 40
    - path: "frontend/components/blocks/BlockRegistry.tsx"
      provides: "Updated attractions renderer mapping to ResortCards"
      contains: "ResortCards"
  key_links:
    - from: "frontend/components/blocks/BlockRegistry.tsx"
      to: "frontend/components/ResortCards.tsx"
      via: "BLOCK_RENDERERS attractions entry"
      pattern: "attractions.*ResortCards"
    - from: "frontend/components/ResortCards.tsx"
      to: "/images/"
      via: "deterministic RESORT_IMAGE_MAP lookup"
      pattern: "RESORT_IMAGE_MAP"
---

<objective>
Create the ResortCards component that replaces the flat bullet list for travel attractions, wire it into BlockRegistry, and establish the test scaffold for all Phase 24 travel UI requirements.

Purpose: The "Must-See Attractions" section currently renders as a plain list with pin icons via ListBlock. This plan transforms it into visual cards with hero images matching the product card quality bar. The test scaffold ensures all Phase 24 requirements have automated verification from the start (Nyquist rule).

Output: ResortCards.tsx component, updated BlockRegistry.tsx attractions mapping, travelUI.test.tsx test file
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-travel-response-ui-overhaul/24-CONTEXT.md
@.planning/phases/24-travel-response-ui-overhaul/24-RESEARCH.md

@frontend/components/blocks/BlockRegistry.tsx
@frontend/components/ListBlock.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/lib/normalizeBlocks.ts:
```typescript
export interface NormalizedBlock {
  type: string
  title?: string
  data: unknown
}
```

From frontend/components/blocks/BlockRegistry.tsx (attractions entry to replace):
```typescript
// Current — swap this to use ResortCards:
attractions: (b) => (
    <ListBlock title={b.title ?? 'Must-See Attractions'} items={(b.data as string[]) ?? []} type="attractions" />
),
```

From backend travel_compose.py (data shape):
```python
# attractions is List[str] — plain strings, no image URLs, no prices
ui_blocks.append({
    "type": "attractions",
    "title": "Must-See Attractions",
    "data": ["Seven Mile Beach, Negril", "Dunn's River Falls", ...]
})
```

Existing fallback images available:
- /images/products/fallback-hotel.webp (49KB, beach/hotel scene)
- /images/categories/cat-travel.webp (editorial travel image)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create travelUI test scaffold</name>
  <files>frontend/tests/travelUI.test.tsx</files>
  <behavior>
    - TRVL-01: ResortCards renders attraction strings as card elements (not bullet list items)
    - TRVL-02: ResortCards falls back to generic image when no resort name matches the lookup map
    - TRVL-03: HotelCards PLPLinkCard renders a hero image element (img tag present)
    - TRVL-04: HotelCards PLPLinkCard CTA button text reads "Search on Expedia" (not "Search Properties")
    - TRVL-05: FlightCards PLPLinkCard CTA button text reads "Search on Expedia" (not "Search Flights")
    - TRVL-06: FlightCards PLPLinkCard destination text does not use tracking-wider class (Caribbean wrap fix)
    - TRVL-07: BlockRegistry conclusion block renders with a tinted background style (color-mix)
  </behavior>
  <action>
    Create `frontend/tests/travelUI.test.tsx` with Vitest + React Testing Library tests covering TRVL-01 through TRVL-07.

    Follow the project's existing test patterns (see `frontend/tests/chatScreen.test.tsx` or `frontend/tests/blockRegistryTopPick.test.tsx` for mocking conventions).

    For TRVL-01 and TRVL-02: Import ResortCards directly, render with sample attraction strings like `["Seven Mile Beach, Negril", "Unknown Resort XYZ"]`. Assert card elements exist (not `<li>` bullet items), assert image elements present, assert the unknown resort falls back to the fallback image path.

    For TRVL-03 through TRVL-06: Import HotelCards and FlightCards, render with mock PLPLink data. Assert the img element exists in hotel card, assert CTA button text matches "Search on Expedia", assert flight destination span does NOT have `tracking-wider` class.

    For TRVL-07: Import and render the conclusion block through BlockRegistry or directly test the renderer output. Assert the container has `color-mix` in its inline style.

    Mark tests TRVL-01 and TRVL-02 as the RED phase (they will fail until ResortCards is created in Task 2). Tests TRVL-03 through TRVL-07 will also be RED until Plan 02 executes.

    Use `vi.mock('next/navigation', ...)` and `vi.mock('@/lib/trackAffiliate', ...)` as needed (follow existing test mocking patterns).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/travelUI.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 7 test cases. Tests TRVL-01 and TRVL-02 initially fail (RED). After Task 2 completes, TRVL-01 and TRVL-02 pass GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Create ResortCards component and swap BlockRegistry renderer</name>
  <files>frontend/components/ResortCards.tsx, frontend/components/blocks/BlockRegistry.tsx</files>
  <action>
    **ResortCards.tsx:**

    Create `frontend/components/ResortCards.tsx` as a new component with this contract:

    ```typescript
    interface ResortCardsProps {
      title?: string
      items: string[]   // plain strings from backend: "Seven Mile Beach, Negril"
    }
    ```

    Implementation details:
    1. Add `'use client'` directive at top.
    2. Create a static `RESORT_IMAGE_MAP: Record<string, string>` mapping lowercase name fragments to image paths. Pre-populate with Caribbean-specific entries that will match common attraction names:
       - `'seven mile'` -> `/images/products/fallback-hotel.webp`
       - `'dunn'` -> `/images/categories/cat-travel.webp`
       - `'blue mountain'` -> `/images/categories/cat-travel.webp`
       - `'rick'` -> `/images/products/fallback-hotel.webp`
       - `'negril'` -> `/images/products/fallback-hotel.webp`
       - `'montego'` -> `/images/categories/cat-travel.webp`
       - `'ocho rios'` -> `/images/products/fallback-hotel.webp`
       - `'bob marley'` -> `/images/categories/cat-travel.webp`
       These will be upgraded to proper AI-generated images in Plan 03. Use existing fallback images as placeholders.
    3. Create a deterministic `getResortImage(name: string): string` function that loops through RESORT_IMAGE_MAP keys, checking `name.toLowerCase().includes(key)`. If no match, return `/images/products/fallback-hotel.webp` as the generic fallback. NO Math.random().
    4. Render a section header with the MapPin icon from lucide-react: `<h3 className="font-sans font-bold text-lg sm:text-xl text-[var(--text)] flex items-center gap-2.5 mb-4">` with a MapPin icon in an amber-tinted background pill (matching ListBlock's attractions style).
    5. Render cards in `<div className="grid grid-cols-1 sm:grid-cols-2 gap-3 sm:gap-4">`.
    6. Each card structure:
       - Outer: `<div className="border border-[var(--border)] rounded-xl overflow-clip bg-[var(--surface)] shadow-card product-card-hover transition-all">` (use `overflow-clip` not `overflow-hidden` per Phase 23 decision)
       - Hero image: `<div className="relative h-[140px] sm:h-[160px]">` containing `<img src={imageUrl} alt="" aria-hidden="true" className="w-full h-full object-cover" loading={idx === 0 ? "eager" : "lazy"} />` (first image eager for LCP, rest lazy). Use raw `<img>` tag per project convention (not next/image).
       - Content section: `<div className="p-4">` with:
         - Resort name: `<h4 className="font-sans font-bold text-base sm:text-lg text-[var(--text)] leading-tight line-clamp-2">{name}</h4>`
         - Location hint (extract from comma-separated name if present): lighter text below name
       - No star rating or price (backend doesn't provide these -- PRD says "if available", safe to omit)
       - No CTA button in v1 (no booking URL available from backend string data)
    7. Touch targets: ensure the card itself has min-height of 44px (the card is taller than this anyway).
    8. Guard: `if (!items || items.length === 0) return null`

    **BlockRegistry.tsx:**

    1. Add import at top: `import ResortCards from '@/components/ResortCards'`
    2. Change ONLY the `attractions` entry in BLOCK_RENDERERS from:
       ```
       attractions: (b) => (
           <ListBlock title={b.title ?? 'Must-See Attractions'} items={(b.data as string[]) ?? []} type="attractions" />
       ),
       ```
       to:
       ```
       attractions: (b) => (
           <ResortCards title={b.title ?? 'Must-See Attractions'} items={(b.data as string[]) ?? []} />
       ),
       ```
    3. Keep the `ListBlock` import (still used by `activities` and `restaurants`).
    4. Do NOT touch UIBlocks function, travel grid logic, or any other renderer.

    After implementation, run travelUI tests -- TRVL-01 and TRVL-02 should now pass GREEN.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/travelUI.test.tsx -t "TRVL-01|TRVL-02" 2>&1 | tail -15</automated>
  </verify>
  <done>ResortCards.tsx renders attraction strings as visual cards with hero images. BlockRegistry dispatches attractions block type to ResortCards instead of ListBlock. TRVL-01 and TRVL-02 tests pass. Activities and restaurants still render via ListBlock (no regression).</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run tests/travelUI.test.tsx` -- TRVL-01 and TRVL-02 pass
- `cd frontend && npm run build` -- no TypeScript errors, no build failures
- Grep confirms `attractions` entry in BlockRegistry uses `ResortCards` not `ListBlock`
- Grep confirms NO `Math.random()` in ResortCards.tsx
- Grep confirms `activities` and `restaurants` entries in BlockRegistry still use `ListBlock`
</verification>

<success_criteria>
- ResortCards component exists and renders attraction name strings as visual cards with images
- BlockRegistry dispatches the `attractions` block type to ResortCards
- Image selection is deterministic (name-fragment matching, no randomness)
- First card image uses loading="eager", others use loading="lazy"
- Test scaffold covers TRVL-01 through TRVL-07 (TRVL-01, TRVL-02 green; rest red until Plan 02)
- No regression: activities and restaurants still use ListBlock
</success_criteria>

<output>
After completion, create `.planning/phases/24-travel-response-ui-overhaul/24-01-SUMMARY.md`
</output>
