---
phase: 24-travel-response-ui-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/HotelCards.tsx
  - frontend/components/FlightCards.tsx
  - frontend/components/DestinationInfo.tsx
  - frontend/components/ItineraryView.tsx
  - frontend/components/ListBlock.tsx
  - frontend/components/blocks/BlockRegistry.tsx
autonomous: true
requirements: [TRV-02, TRV-03, TRV-04, TRV-05]

must_haves:
  truths:
    - "Hotel PLPLinkCard shows a destination hero image at the top of the card"
    - "Flight PLPLinkCard shows a destination hero image at the top of the card"
    - "Hotel CTA reads 'Search on Expedia' instead of 'Search Properties'"
    - "Flight CTA reads 'Search on Expedia' instead of 'Search Flights'"
    - "Caribbean destination text does not wrap mid-word on mobile"
    - "All travel section headers use sans-serif font (no font-serif class)"
    - "Powered by Expedia badge is visually smaller/subtler"
    - "Conclusion block has a tinted background treatment"
  artifacts:
    - path: "frontend/components/HotelCards.tsx"
      provides: "PLPLinkCard with hero image, updated CTA label, smaller provider badge"
      contains: "Search on Expedia"
    - path: "frontend/components/FlightCards.tsx"
      provides: "PLPLinkCard with hero image, updated CTA label, fixed Caribbean text wrap"
      contains: "Search on Expedia"
    - path: "frontend/components/DestinationInfo.tsx"
      provides: "Sans-serif section headers"
      contains: "font-sans font-bold"
    - path: "frontend/components/ItineraryView.tsx"
      provides: "Sans-serif section headers"
      contains: "font-sans font-bold"
    - path: "frontend/components/blocks/BlockRegistry.tsx"
      provides: "Styled conclusion block renderer"
      contains: "color-mix"
  key_links:
    - from: "frontend/components/HotelCards.tsx"
      to: "/images/products/fallback-hotel.webp"
      via: "hero image src"
      pattern: "fallback-hotel"
    - from: "frontend/components/FlightCards.tsx"
      to: "/images/products/fallback-flight.webp"
      via: "hero image src"
      pattern: "fallback-flight"
---

<objective>
Overhaul hotel and flight PLPLinkCards with destination hero images, fix the Caribbean text wrapping bug, update CTA labels, and apply typography/spacing cleanup across all travel components.

Purpose: The hotel and flight widgets currently show only a large Search icon placeholder with no destination imagery -- they look like empty boxes. The "Caribbean" text wraps mid-word on mobile due to tracking-wider. Section headers use a decorative serif font that clashes with the body text. The conclusion block lacks visual distinction. This plan addresses all four issues.

Output: Updated HotelCards.tsx, FlightCards.tsx, DestinationInfo.tsx, ItineraryView.tsx, ListBlock.tsx, BlockRegistry.tsx (conclusion renderer only)
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-travel-response-ui-overhaul/24-CONTEXT.md
@.planning/phases/24-travel-response-ui-overhaul/24-RESEARCH.md

@frontend/components/HotelCards.tsx
@frontend/components/FlightCards.tsx
@frontend/components/DestinationInfo.tsx
@frontend/components/ItineraryView.tsx
@frontend/components/ListBlock.tsx
@frontend/components/blocks/BlockRegistry.tsx

<interfaces>
<!-- Current component structures the executor needs to understand -->

HotelCards.tsx PLPLinkCard (lines 45-115):
- Currently: centered flex-col layout with Search icon, font-serif h4, "Search Properties" CTA
- `product-card-hover` class exists in globals.css -- safe to keep
- Uses `trackAffiliateClick` from `@/lib/trackAffiliate`

FlightCards.tsx PLPLinkCard (lines 49-137):
- Currently: centered flex-col layout with Search icon, font-serif h4, "Search Flights" CTA
- Route visualization row (lines 93-105): `text-xl font-bold tracking-wider` on origin + destination spans causes wrapping
- `flight.origin` and `flight.destination` are strings like "NYC" and "Caribbean"

BlockRegistry.tsx conclusion renderer (lines 134-138):
```typescript
conclusion: (b) => (
    <div className="mt-4 px-3 sm:px-4 py-3 rounded-2xl rounded-tl-md bg-[var(--surface-elevated)] border border-[var(--border)] text-[14px] sm:text-[15px] leading-relaxed text-[var(--text)]">
        {(b.data as any)?.text}
    </div>
),
```

font-serif occurrences to change:
- HotelCards.tsx line 82: h4 in PLPLinkCard
- HotelCards.tsx line 162: h4 in TraditionalHotelCard  
- HotelCards.tsx line 222: h3 section title
- FlightCards.tsx line 88: h4 in PLPLinkCard
- FlightCards.tsx line 238: h3 section title
- DestinationInfo.tsx lines 30, 52, 74, 101: h4 section headers
- ItineraryView.tsx lines 40, 64: h3/h4 headers
- ListBlock.tsx line 50: h3 title
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hotel and Flight PLPLinkCard overhaul</name>
  <files>frontend/components/HotelCards.tsx, frontend/components/FlightCards.tsx</files>
  <action>
    **HotelCards.tsx -- PLPLinkCard component (lines 45-115):**

    1. **Add hero image section** at the top of the card, BEFORE the existing content. Remove the centered icon layout and restructure:
       - Remove the `flex flex-col items-center text-center` wrapper. Replace with a top image band + content block below.
       - Hero image section: `<div className="relative h-[120px] sm:h-[140px] overflow-clip">` containing:
         ```
         <img src="/images/products/fallback-hotel.webp" alt="" aria-hidden="true" className="w-full h-full object-cover" loading="lazy" />
         <div className="absolute inset-0 bg-gradient-to-t from-black/30 to-transparent" />
         ```
       - Remove the `w-16 h-16 rounded-full` search icon circle entirely.
       - Content below image: `<div className="p-5">` with left-aligned text (not centered).

    2. **Update title h4:** Change from `font-serif font-semibold text-xl` to `font-sans font-bold text-lg sm:text-xl`. Keep `text-[var(--text)]`.

    3. **Shrink provider badge:** Change from `text-xs px-3 py-1.5 rounded-full mb-6` to `text-[10px] px-2 py-1 rounded-full mb-4 opacity-60`. Keep border and color classes. This reduces visual weight per PRD.

    4. **Update CTA button text:** Change `Search Properties` to `Search on Expedia`. Keep ExternalLink icon, keep `w-full px-5 py-3` sizing (44px+ touch target preserved).

    5. **Update outer card wrapper:** Change `p-8` to `p-0 overflow-clip` (padding now handled by hero image + content div). Keep `rounded-xl` and `product-card-hover`.

    6. **HotelCards section title (h3 around line 222):** Change `font-serif text-2xl font-semibold` to `font-sans font-bold text-xl`. Keep icon + gap + text-[var(--text)].

    7. **TraditionalHotelCard h4 (line 162):** Change `font-serif font-semibold text-xl` to `font-sans font-bold text-lg`. Keep other classes.

    **FlightCards.tsx -- PLPLinkCard component (lines 49-137):**

    1. **Add hero image section** identical pattern to HotelCards but using flight image:
       - Hero image: `<img src="/images/products/fallback-flight.webp" ...>` with same gradient overlay.
       - Remove the `w-16 h-16 rounded-full` search icon circle.
       - Restructure from centered layout to image-top + content-below.

    2. **Fix Caribbean text wrapping bug (CRITICAL):** In the route visualization row (lines 93-105), change the origin and destination `<span>` elements:
       - Remove `tracking-wider` class from both spans (this expands letter-spacing causing overflow).
       - Change `text-xl font-bold` to `text-base sm:text-lg font-bold` for slightly smaller text on mobile.
       - Add `whitespace-nowrap` to prevent mid-word breaks.
       - The spans should be: `<span className="text-base sm:text-lg font-bold whitespace-nowrap text-[var(--text)]">`

    3. **Update title h4:** Change `font-serif text-xl` to `font-sans font-bold text-lg sm:text-xl`. Keep `text-[var(--text)]`.

    4. **Shrink provider badge:** Same treatment as HotelCards -- `text-[10px] px-2 py-1 rounded-full mb-4 opacity-60`.

    5. **Update CTA button text:** Change `Search Flights` to `Search on Expedia`. Keep ExternalLink icon and sizing.

    6. **Update outer card wrapper:** `p-0 overflow-clip` instead of `p-8`. Keep `rounded-xl`.

    7. **FlightCards section title (h3 around line 238):** Change `font-serif text-2xl` to `font-sans font-bold text-xl`. Keep icon + gap.

    After implementation, run the travelUI tests from Plan 01 -- TRVL-03 through TRVL-06 should now pass.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/travelUI.test.tsx -t "TRVL-03|TRVL-04|TRVL-05|TRVL-06" 2>&1 | tail -15</automated>
  </verify>
  <done>Hotel and flight PLPLinkCards render with hero destination images at the top. CTA buttons read "Search on Expedia". Flight destination text uses whitespace-nowrap without tracking-wider (Caribbean no longer wraps). Provider badge is smaller. All four TRVL tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Typography cleanup, conclusion block, and spacing polish</name>
  <files>frontend/components/DestinationInfo.tsx, frontend/components/ItineraryView.tsx, frontend/components/ListBlock.tsx, frontend/components/blocks/BlockRegistry.tsx</files>
  <action>
    **DestinationInfo.tsx -- replace font-serif on all h4 elements:**

    Change all 4 h4 headers (Weather & Climate, Best Time to Visit, Travel Tips, Local Customs & Etiquette) from:
    ```
    <h4 className="font-serif font-bold text-base mb-1 text-[var(--text)]">
    ```
    to:
    ```
    <h4 className="font-sans font-bold text-[15px] text-[var(--text)] mb-1">
    ```
    The Travel Tips h4 has `mb-3` instead of `mb-1` -- preserve that difference. Change only `font-serif` to `font-sans` and `text-base` to `text-[15px]` on that one.

    **ItineraryView.tsx -- replace font-serif on h3 and h4 elements:**

    Line 40 (main heading): Change `font-serif text-xl` to `font-sans font-bold text-xl`. Keep `text-[var(--text)]`, `flex items-center gap-2`.

    Line 64 (day title): Change `font-serif text-lg` to `font-sans font-bold text-lg`. Keep `text-[var(--text)]`.

    **ListBlock.tsx -- replace font-serif on h3:**

    Line 50: Change `font-serif font-bold text-lg` to `font-sans font-bold text-lg`. This affects activities and restaurants list headers (attractions now use ResortCards from Plan 01).

    **BlockRegistry.tsx -- style conclusion block:**

    Replace the `conclusion` renderer (lines 134-138) with a styled version that adds a tinted background:
    ```typescript
    conclusion: (b) => (
        <div
            className="mt-2 p-4 rounded-xl border border-[var(--border)] text-[14px] sm:text-[15px] leading-relaxed text-[var(--text-secondary)]"
            style={{ background: 'color-mix(in srgb, var(--primary) 6%, var(--surface))' }}
        >
            {(b.data as any)?.text}
        </div>
    ),
    ```

    Key changes from current:
    - `mt-4` -> `mt-2` (reduce dead space)
    - `px-3 sm:px-4 py-3 rounded-2xl rounded-tl-md` -> `p-4 rounded-xl` (uniform padding, consistent rounding)
    - `bg-[var(--surface-elevated)]` -> inline `style={{ background: 'color-mix(in srgb, var(--primary) 6%, var(--surface))' }}` (subtle primary tint per PRD)
    - `text-[var(--text)]` -> `text-[var(--text-secondary)]` (secondary tone for the prompt text)

    Do NOT modify any other renderer in BLOCK_RENDERERS. Do NOT touch UIBlocks function. The only BlockRegistry change is the conclusion renderer styling.

    NOTE: Plan 01 also modifies BlockRegistry.tsx (attractions renderer swap). These changes are to DIFFERENT entries in BLOCK_RENDERERS. If Plan 01 has already executed, preserve the ResortCards import and attractions entry. If executing in parallel, the executor should merge both changes.

    After implementation, run the travelUI test for TRVL-07.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/travelUI.test.tsx -t "TRVL-07" 2>&1 | tail -10</automated>
  </verify>
  <done>All travel section headers use font-sans font-bold (no font-serif anywhere in HotelCards, FlightCards, DestinationInfo, ItineraryView, ListBlock). Conclusion block renders with a color-mix tinted background. TRVL-07 test passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run tests/travelUI.test.tsx` -- TRVL-03 through TRVL-07 all pass
- `cd frontend && npm run build` -- no TypeScript errors
- `grep -rn "font-serif" frontend/components/HotelCards.tsx frontend/components/FlightCards.tsx frontend/components/DestinationInfo.tsx frontend/components/ItineraryView.tsx frontend/components/ListBlock.tsx` -- returns NO results
- `grep -n "Search Properties" frontend/components/HotelCards.tsx` -- returns NO results
- `grep -n "Search Flights" frontend/components/FlightCards.tsx` -- returns NO results
- `grep -n "tracking-wider" frontend/components/FlightCards.tsx` -- returns NO results (Caribbean fix confirmed)
- `grep -n "color-mix" frontend/components/blocks/BlockRegistry.tsx` -- returns result in conclusion renderer
</verification>

<success_criteria>
- Hotel PLPLinkCard has a hero image at the top (fallback-hotel.webp)
- Flight PLPLinkCard has a hero image at the top (fallback-flight.webp)
- Both CTAs read "Search on Expedia"
- Caribbean text wrapping is fixed (no tracking-wider, whitespace-nowrap added)
- Provider badge is smaller (text-[10px], opacity-60)
- All font-serif occurrences removed from travel components
- Conclusion block has color-mix tinted background
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-travel-response-ui-overhaul/24-02-SUMMARY.md`
</output>
