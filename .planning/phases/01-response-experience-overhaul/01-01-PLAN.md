---
phase: 01-response-experience-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_product_affiliate.py
  - backend/tests/test_product_compose.py
  - backend/tests/test_chat_streaming.py
  - backend/tests/test_review_search.py
autonomous: true
requirements:
  - RX-01
  - RX-02
  - RX-03
  - RX-04
  - RX-05
  - RX-06
  - RX-07
  - RX-08

must_haves:
  truths:
    - "All 8 RX requirements have failing test stubs that can be run without errors"
    - "Tests fail because the features are not yet implemented (RED state confirmed)"
    - "Test infrastructure compiles and pytest collects all test cases"
  artifacts:
    - path: "backend/tests/test_product_affiliate.py"
      provides: "Test stubs for RX-03 (parallel per-product search) and RX-05 (fast path plan)"
      contains: "test_affiliate_search_products_parallel_within_provider"
    - path: "backend/tests/test_product_compose.py"
      provides: "Test stubs for RX-06, RX-07, RX-08"
      contains: "test_no_opener_call, test_blog_includes_source_inline_links, test_stream_chunk_data_set_by_product_affiliate"
    - path: "backend/tests/test_chat_streaming.py"
      provides: "Test stubs for RX-01, RX-02"
      contains: "test_product_cards_emitted_before_compose, test_blog_article_uses_model_service_stream"
    - path: "backend/tests/test_review_search.py"
      provides: "Test stub for RX-04 (review search caps at 3 products)"
      contains: "test_review_search_caps_at_3_products"
  key_links:
    - from: "backend/tests/test_product_affiliate.py"
      to: "backend/mcp_server/tools/product_affiliate.py"
      via: "import product_affiliate"
      pattern: "from mcp_server.tools.product_affiliate import"
    - from: "backend/tests/test_product_compose.py"
      to: "backend/mcp_server/tools/product_compose.py"
      via: "import product_compose"
      pattern: "from mcp_server.tools.product_compose import"
    - from: "backend/tests/test_review_search.py"
      to: "backend/mcp_server/tools/review_search.py"
      via: "import review_search"
      pattern: "from mcp_server.tools.review_search import"
---

<objective>
Create Wave 0 test scaffolds (failing stubs) for all 8 RX requirements.

Purpose: Establish the RED state so subsequent implementation plans can verify GREEN.
Output: Four test files with runnable failing tests covering RX-01 through RX-08.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-response-experience-overhaul/01-RESEARCH.md

@backend/tests/conftest.py
@backend/tests/test_product_compose.py
@backend/tests/test_review_search.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold test_product_affiliate.py with RX-03 and RX-05 stubs, append RX-04 stub to test_review_search.py</name>
  <files>backend/tests/test_product_affiliate.py, backend/tests/test_review_search.py</files>
  <behavior>
    - Test: test_affiliate_search_products_parallel_within_provider — given 3 products to search on one provider, the search coroutines must be gathered with asyncio.gather (not run in a for loop). STUB: pytest.fail("RX-03: per-product parallelism not yet implemented")
    - Test: test_planner_fast_path_review_and_affiliate_in_same_step — the `_create_fast_path_product_plan` method must put review_search and product_affiliate in the SAME step with parallel=True. STUB: pytest.fail("RX-05: parallel step not yet in fast_path plan")
    - Test: test_review_search_caps_at_3_products — review_search must cap results at 3 products (not 5). STUB: pytest.fail("RX-04: review_search still uses [:5]")
  </behavior>
  <action>
    PART A — Create `backend/tests/test_product_affiliate.py`. Model after the env bootstrap pattern in `test_product_compose.py` (lines 1-22: setdefault ENV, SECRET_KEY, DATABASE_URL, etc. before any app import).

    Write two test stubs:

    1. `test_affiliate_search_products_parallel_within_provider`:
       - Patch `affiliate_manager.get_available_providers()` to return `["mock_provider"]`
       - Patch `affiliate_manager.get_provider("mock_provider")` to return a mock provider where `search_products` is an `AsyncMock` that returns a list of one result object
       - Call `product_affiliate(state)` with `normalized_products=[{"title": "Product A"}, {"title": "Product B"}, {"title": "Product C"}]`
       - STUB BODY: `pytest.fail("RX-03: per-product parallelism not yet implemented — search_provider() uses a for loop")`

    2. `test_planner_fast_path_review_and_affiliate_in_same_step`:
       - Import `PlannerAgent` from `app.agents.planner_agent`
       - Call `planner._create_fast_path_product_plan(include_extractor=False)`
       - Locate the step that contains `review_search`
       - STUB BODY: `pytest.fail("RX-05: review_search and product_affiliate not in the same parallel step in fast_path plan")`

    Use `@pytest.mark.asyncio` for async tests. Both tests must be XFAIL-style stubs (they fail with a clear message describing the missing feature, not with an import error).

    PART B — Append to `backend/tests/test_review_search.py` (do NOT remove existing content):

    Add one test function at the end of the file:

    ```python
    def test_review_search_caps_at_3_products():
        """RX-04: review_search must limit results to 3 products, not 5."""
        pytest.fail("RX-04: review_search still uses [:5] — update slice to [:3]")
    ```

    This stub is intentionally synchronous — the existing test_review_search.py tests use the same pattern. Confirm the file already has the env bootstrap before any imports and append after the last existing test function.
  </action>
  <verify>
    <automated>cd /c/Users/habib/downloads/reviewguide-sourcecode/backend && python -m pytest tests/test_product_affiliate.py tests/test_review_search.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>Both test_product_affiliate.py stubs FAIL with their stub messages. test_review_search_caps_at_3_products FAILS with its stub message. Existing test_review_search.py tests are unaffected. Exit code 1, no ImportError or SyntaxError.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold test_product_compose.py additions and test_chat_streaming.py</name>
  <files>backend/tests/test_product_compose.py, backend/tests/test_chat_streaming.py</files>
  <behavior>
    In test_product_compose.py (append to existing file):
    - Test: test_no_opener_call — product_compose must NOT call model_service.generate for the 'opener' task when review_data is present. STUB fails.
    - Test: test_no_conclusion_call — product_compose must NOT call model_service.generate for the 'conclusion' task. STUB fails.
    - Test: test_blog_includes_source_inline_links — when review_bundles contain sources with url and site_name, the blog_data string passed to model_service.generate for blog_article must include "Reviews:" with markdown links. STUB fails.
    - Test: test_stream_chunk_data_set_by_product_affiliate — after product_affiliate completes, stream_chunk_data must be set to {"type": "ui_blocks", ...}. STUB fails. (RX-01/RX-08)

    New file test_chat_streaming.py:
    - Test: test_product_cards_emitted_before_compose — RX-01: product cards (ui_blocks) are available in state before product_compose runs. STUB fails.
    - Test: test_blog_article_uses_model_service_stream — RX-02: model_service.generate is called with stream=True for the blog_article task. STUB fails.
  </behavior>
  <action>
    PART A — append to `backend/tests/test_product_compose.py` (do NOT remove existing content):

    Add four test functions after the existing test. Each has a minimal state dict and a `pytest.fail(...)` call identifying the requirement and what is missing.

    For `test_no_opener_call` and `test_no_conclusion_call`: build a minimal state with `review_data={"Product A": {"sources": [{"site_name": "Wirecutter", "url": "https://wirecutter.com", "snippet": "great"}], "avg_rating": 4.5, "total_reviews": 100, "quality_score": 2.9, "source_type_count": 2}}` and non-empty `affiliate_products`. Patch `model_service.generate` to record call args. Run `product_compose(state)`. Then: `pytest.fail("RX-06: opener/conclusion calls not yet removed from product_compose")`.

    For `test_blog_includes_source_inline_links`: same state, check the call args for the blog_article call contain "Reviews:". STUB: `pytest.fail("RX-07: source review links not threaded into blog_data")`.

    For `test_stream_chunk_data_set_by_product_affiliate`: keep it simple — create a state dict with `normalized_products=[{"title": "X"}]`, call `product_affiliate(state)`, then check `state.get("stream_chunk_data")`. STUB: `pytest.fail("RX-01/RX-08: product_affiliate does not set stream_chunk_data")`.

    PART B — create `backend/tests/test_chat_streaming.py`:

    Use the same env bootstrap pattern. Do NOT import chat.py directly (it requires full app startup). Instead test the contract at the unit level:

    - `test_product_cards_emitted_before_compose`: Import `PlanExecutor` from `app.services.plan_executor`. Create a minimal executor, set `state["affiliate_products"]` to a non-empty dict. Check `state.get("stream_chunk_data")`. STUB: `pytest.fail("RX-01: plan_executor does not emit artifact callback after product_affiliate step")`.

    - `test_blog_article_uses_model_service_stream`: Import `model_service` from `app.services.model_service`. Patch `model_service.generate` to record `stream` kwarg. Run `product_compose(state)`. Check that `generate` was called with `stream=True` for the blog_article call. STUB: `pytest.fail("RX-02: product_compose does not call model_service.generate(stream=True) for blog_article")`.
  </action>
  <verify>
    <automated>cd /c/Users/habib/downloads/reviewguide-sourcecode/backend && python -m pytest tests/test_product_compose.py tests/test_chat_streaming.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>All new test cases collected and FAIL with their stub messages. No ImportError. Exit code 1. Existing test_compose_uses_general_product_info_when_no_listings still passes.</done>
</task>

</tasks>

<verification>
Run the full new test scaffold:
```
cd backend && python -m pytest tests/test_product_affiliate.py tests/test_product_compose.py tests/test_chat_streaming.py tests/test_review_search.py -v 2>&1 | tail -40
```
Expected: 9+ FAILures (stub messages), 1 PASS (existing test_compose test), existing test_review_search tests pass, 0 ERRORs.
</verification>

<success_criteria>
- test_product_affiliate.py: 2 tests, both FAIL with stub messages
- test_product_compose.py: 1 existing PASS + 4 new FAIL stubs
- test_chat_streaming.py: 2 tests, both FAIL with stub messages
- test_review_search.py: existing tests PASS + 1 new FAIL stub (test_review_search_caps_at_3_products)
- No ImportError or SyntaxError in any file
- All test IDs match the VALIDATION.md map
</success_criteria>

<output>
After completion, create `.planning/phases/01-response-experience-overhaul/01-01-SUMMARY.md`
</output>
