---
phase: 01-response-experience-overhaul
plan: 06
type: execute
wave: 4
depends_on:
  - 01-05
files_modified: []
autonomous: false

requirements:
  - RX-01
  - RX-02
  - RX-03
  - RX-04
  - RX-05
  - RX-06
  - RX-07
  - RX-08

must_haves:
  truths:
    - "Product cards appear within 5 seconds of sending a query"
    - "Blog text streams token-by-token in real time"
    - "Blog text contains inline markdown buy links"
    - "No 90-second thinking wall"
    - "No regressions in response quality"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all 8 RX requirements against the live running stack.

Purpose: Confirm the streaming and parallelism changes produce the intended user experience — product cards within 5s, token-by-token text streaming, inline buy links, and no quality regressions.
Output: Verified or issues logged for gap closure.
</objective>

<execution_context>
@C:/Users/habib/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/habib/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-response-experience-overhaul/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 1: Response Experience Overhaul</name>
  <what-built>
    All 5 plans of Phase 1 executed:
    - Plan 01: Test scaffolds (all RX requirement stubs)
    - Plan 02: Backend parallelism (product_affiliate parallel, review_search 3 products + timeout, parallel step in fast-path plan)
    - Plan 03: product_compose cleanup (removed opener/conclusion LLM calls, source URLs in blog data)
    - Plan 04: Early product card streaming (artifact callback, product_affiliate emits stream_chunk_data, chat.py SSE path)
    - Plan 05: Blog article token streaming (token callback, stream=True for blog_article, fake-chunking suppressed)
  </what-built>
  <how-to-verify>
    Start the full stack:
    ```
    docker-compose up -d
    cd frontend && npm run dev
    ```

    Then run these checks in order:

    **RX-01 + RX-08: Product cards within 5 seconds**
    1. Open http://localhost:3000/chat in browser
    2. Open DevTools → Network tab → filter by "stream"
    3. Send query: "best noise cancelling headphones under $300"
    4. Start a stopwatch when you press Enter
    5. PASS if product cards (eBay/Amazon carousel) appear within 5 seconds
    6. PASS if cards appear BEFORE the blog text starts streaming

    **RX-02: Token-by-token streaming**
    1. After sending the same query, watch the blog text area
    2. PASS if words appear progressively (one or a few at a time) — NOT a single batch dump
    3. PASS if you can read the first sentence before the last sentence arrives
    4. FAIL if all text appears at once after a delay

    **RX-07: Inline affiliate links in blog**
    1. Look at the blog article text that streams in
    2. PASS if the text contains clickable markdown links like "[Check price on eBay →](...)" or "[Check price on Amazon →](...)"
    3. PASS if clicking a link opens the product page in a new tab

    **RX-03 + RX-05: Parallel backend (timing check)**
    1. Check docker-compose logs: `docker-compose logs backend --tail=50`
    2. PASS if you see "Running 2 tools in parallel" in a step that includes review_search and product_affiliate
    3. PASS if you see "Starting parallel search for N products" in product_affiliate logs (all products at once, not one by one)

    **RX-04: Review search reduced**
    1. In backend logs, look for "[review_search] Searching reviews for N products"
    2. PASS if N is 3 (not 5)

    **RX-06: Reduced LLM calls**
    1. In backend logs, look for "[product_compose] Parallel LLM batch:"
    2. PASS if the call list does NOT include "opener" or "conclusion"
    3. PASS if call count is ≤ 5 concurrent calls

    **No regressions**
    1. Send a second query: "compare Sony WH-1000XM5 vs Bose QC45"
    2. PASS if response includes product names, prices, and review summaries
    3. PASS if both products appear in the response
    4. Check http://localhost:3000/browse — PASS if page loads normally
  </how-to-verify>
  <action>Run full stack and verify all RX requirements against live application per the how-to-verify steps.</action>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe specific failures (e.g., "RX-02 fail: text still dumps in batch", "RX-07 fail: no inline links").
    Failed checks will trigger gap closure plans.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification above covers all 8 RX requirements.
Automated full suite as pre-check:
```
cd /c/Users/habib/downloads/reviewguide-sourcecode/backend && python -m pytest tests/ -q --timeout=30 2>&1 | tail -10
```
All tests must be green before proceeding to human verification.
</verification>

<success_criteria>
- All 8 RX requirements verified by human against running stack
- User types "approved" or specific failures documented for gap closure
- Full automated test suite is green
</success_criteria>

<output>
After completion, create `.planning/phases/01-response-experience-overhaul/01-06-SUMMARY.md`
</output>
